## Decisions Verification
- [x] 🔴 **TODO-DEC-001**: Create ADR for the `AzureAuthenticator` utility class design — `connection_manager.py` imports `AzureAuthenticator` from `utils/auth.py` but never calls it (only `DefaultAzureCredential` is used directly in `authenticate()`), yet `AzureAuthenticator` is exported in `utils/__init__.py` as a first-class public API. There is no ADR explaining why two parallel, non-integrated authentication paths exist or which callers are expected to use `AzureAuthenticator`.
- [x] 🔴 **TODO-DEC-002**: Create ADR for the `Config.save()` method design — `config.py` includes a `save(output_file)` method that serialises the in-memory config dict (including plaintext `client_secret`) back to a YAML file. This is a significant security decision (writing secrets to disk) that is completely absent from ADR-006 and ADR-009, with no documented rationale, threat model, or intended use case.
- [x] 🔴 **TODO-DEC-003**: Create ADR for the `utils/` sub-package boundary decision — ADR-003 mentions the utils sub-package exists for "cross-cutting concerns" but does not document why `auth.py` and `logger.py` were separated into a sub-package rather than placed directly in `avd_manager/`, nor why `setup_file_logging()` was kept as a module-level function while `get_logger()` was also a module-level function rather than a class.
- [x] ⚠️ **TODO-DEC-004**: Create ADR for the `monitor_sessions()` N+1 query design — `session_handler.py` implements `monitor_sessions()` as an explicit loop that calls `get_user_sessions()` once per session host, producing N+1 Azure API calls. ADR-012 acknowledges this pattern but does not document whether a bulk/paginated API alternative was evaluated or why the sequential loop was chosen over concurrent execution (e.g., `concurrent.futures.ThreadPoolExecutor`).
- [x] ⚠️ **TODO-DEC-005**: Create ADR for the `disconnect()` implementation strategy — `connection_manager.py`'s `disconnect()` method nullifies all four client references (`avd_client`, `compute_client`, `network_client`, `credential`) but does not call any SDK-level close or cleanup method. There is no ADR documenting whether the Azure SDK clients require explicit teardown, or whether this null-assignment approach is sufficient and intentional.
- [x] ⚠️ **TODO-DEC-006**: Create ADR for the `host.name.split('/')[-1]` ARM ID parsing pattern — both `session_handler.py` (`monitor_sessions`) and `basic_usage.py` (`pool_name = host_pools[0].name.split('/')[-1]`) parse ARM resource IDs by splitting on `/` and taking the last segment. This is an undocumented, fragile string-parsing decision with no ADR explaining why the ARM SDK's own resource ID parsing utilities were not used.
- [x] 📝 **TODO-DEC-007**: Create ADR for the `python-dotenv` / `load_dotenv()` placement decision — `load_dotenv()` is called unconditionally inside `Config.__init__()`, meaning it runs on every `Config` instantiation. There is no ADR explaining why `load_dotenv()` was embedded in the constructor rather than called once at the application entry point, and what the implications are for library consumers who do not want `.env` file side effects.
- [x] 🔴 **TODO-DEC-008**: Document the rationale for including `requests==2.31.0` as a pinned dependency — `requests` is listed in both `requirements.txt` and `setup.py install_requires` but is not imported in any of the 7 source `.py` files. ADR-002 notes this but marks it as "speculative." The ADR should be updated to either document the planned future use case that justifies the dependency, or record a decision to remove it.
- [x] 🔴 **TODO-DEC-009**: Document the `from datetime import datetime` unused import decision in `session_handler.py` — `datetime` is imported at the top of `session_handler.py` but is never referenced anywhere in the module. This is an undocumented dead import. The ADR or code should clarify whether this was intended for a `monitor_sessions()` timestamp feature that was not implemented, and whether it should be removed.
- [x] ⚠️ **TODO-DEC-010**: Document the YAML library choice (`pyyaml`) over alternatives — ADR-006 documents that `yaml.safe_load()` is used (correctly noting the security rationale over `yaml.load()`), but does not document why `pyyaml` was chosen over alternatives such as `ruamel.yaml` (which preserves comments and round-trips YAML) or `strictyaml` (which enforces a schema). This is especially relevant given that `Config.save()` writes YAML back to disk, losing any comments from the original file.
- [x] ⚠️ **TODO-DEC-011**: Document the `python-dotenv` choice over alternatives for `.env` file loading — ADR-006 mentions `python-dotenv` is used but does not document why it was chosen over alternatives (`environs`, `decouple`, or simply `os.environ`), nor does it document the `override=False` default behaviour of `load_dotenv()` (which means existing environment variables are NOT overridden by `.env` file values — the opposite of the YAML-vs-env precedence rule documented in ADR-006).
- [x] ⚠️ **TODO-DEC-012**: Document the `setuptools.find_packages()` scope decision — `find_packages()` in `setup.py` is called without an `exclude` argument, meaning the `tests/` package and `examples/` directory are included in the built distribution. ADR-010 does not document this gap or whether test files being bundled into the installable package was an intentional or accidental decision.
- [x] 📝 **TODO-DEC-013**: Document the `logging.StreamHandler(sys.stdout)` choice over `sys.stderr` — `logger.py` routes all log output to `sys.stdout` rather than the conventional `sys.stderr`. This is an undocumented deviation from Python logging convention (where `StreamHandler` defaults to `sys.stderr`) that affects how consumers redirect or capture log output in shell pipelines and CI environments.
- [x] 📝 **TODO-DEC-014**: Document the `setup.py` `long_description` and `url` omission — `setup.py` contains no `long_description`, `long_description_content_type`, or `url` fields. ADR-010 does not document whether this was a deliberate choice (e.g., not yet ready for PyPI publication) or an oversight, which affects the package's discoverability and presentation on PyPI.
- [x] 🔴 **TODO-DEC-015**: Document the Strategy pattern in `AzureAuthenticator.get_credential()` vs. the direct `DefaultAzureCredential` usage in `ConnectionManager.authenticate()` — ADR-007 describes both paths but does not document the intended design: is `AzureAuthenticator` meant to be the canonical strategy selector that `ConnectionManager` should delegate to, or is `ConnectionManager.authenticate()` the intended primary path? The duplication creates an undocumented architectural inconsistency.
- [x] 🔴 **TODO-DEC-016**: Document the absence of a Context Manager (`__enter__`/`__exit__`) pattern for `ConnectionManager` — `ConnectionManager` manages Azure SDK client lifecycle (connect/disconnect) but does not implement Python's context manager protocol. ADR-004 and ADR-005 do not document why `with ConnectionManager(...) as manager:` usage was not supported, which would guarantee `disconnect()` is called even on exceptions (as shown in `basic_usage.py` where `disconnect()` is only called on the happy path).
- [x] ⚠️ **TODO-DEC-017**: Document the Template Method vs. direct implementation choice in `SessionHandler` — `SessionHandler` has five public methods (`list_session_hosts`, `get_user_sessions`, `disconnect_user_session`, `get_session_host_status`, `monitor_sessions`) that all share an identical pre-condition check pattern (`if not self.connection_manager.avd_client: raise ConnectionError`). ADR-009 does not document why this guard was not extracted into a shared decorator, base class method, or template method to avoid the repeated pattern.
- [x] ⚠️ **TODO-DEC-018**: Document the decision to expose `active_sessions = {}` as a public instance attribute on `SessionHandler` — `session_handler.py` initialises `self.active_sessions = {}` in `__init__` but no method ever writes to it. ADR-012 notes this but does not document whether this was a placeholder for a future caching pattern, a remnant of a removed feature, or an intentional hook for subclasses. Its presence as a public attribute creates an implicit, undocumented API contract.
- [x] 📝 **TODO-DEC-019**: Document the Singleton-avoidance decision for `get_logger()` — `logger.py` uses `logging.getLogger(name)` (which returns the same logger instance for the same name from Python's global registry) combined with a `if not logger.handlers:` guard. ADR-008 does not document that this effectively makes each named logger a singleton via Python's logging registry, nor does it document the thread-safety implications of the handler-check guard in multi-threaded consumers.
- [x] 🔴 **TODO-DEC-020**: Expand ADR-009 trade-off analysis for `get_host_pool()` returning `None` on failure — `connection_manager.py`'s `get_host_pool()` returns `None` on API failure, but `basic_usage.py` calls `pool_details.name` on the return value without a `None` check (`pool_details = manager.get_host_pool(pool_name)` followed immediately by `print(f"Host Pool Details: {pool_details.name}")`). ADR-009 does not document this specific inconsistency between the stated error-handling policy and the example code that ships with the library.
- [x] 🔴 **TODO-DEC-021**: Document the trade-off of `Config.validate()` only checking two of five fields — `config.py`'s `validate()` enforces only `subscription_id` and `resource_group` as required, leaving `tenant_id`, `client_id`, and `client_secret` unchecked. ADR-006 notes this but does not document the explicit decision: was a two-tier validation model (infrastructure vs. auth fields) intentional, and if so, where should auth-field validation live?
- [x] ⚠️ **TODO-DEC-022**: Document the `compute_client` and `network_client` over-provisioning trade-off in ADR-004 — `connection_manager.py` instantiates `ComputeManagementClient` and `NetworkManagementClient` in `connect()`, but neither client is used in any current method. ADR-004 acknowledges this but does not document the cost: each unused client instantiation triggers an SDK initialisation and may perform network activity (e.g., metadata endpoint discovery), adding latency to every `connect()` call for zero current benefit.
- [x] ⚠️ **TODO-DEC-023**: Document the `setup_file_logging()` root-logger attachment trade-off in ADR-008 — `logger.py`'s `setup_file_logging()` attaches a `FileHandler` to `logging.getLogger()` (the root logger), which captures all log output from all libraries loaded in the process (Azure SDK, urllib3, etc.), not just `avd_manager` loggers. ADR-008 mentions this but does not document the decision to use the root logger vs. attaching the file handler to the `avd_manager` package logger specifically.
- [x] ⚠️ **TODO-DEC-024**: Document the `pyproject.toml` migration trade-off in ADR-010 — ADR-010 notes that `setup.py` is the legacy format but does not document the specific migration path or timeline to `pyproject.toml`. The `setup.py` also lacks a `[build-system]` table, meaning PEP 517 build frontends (`build`, `pip` with `--no-build-isolation`) may behave differently than expected. The trade-off between migration effort and build-tool compatibility should be explicitly recorded.
- [x] 📝 **TODO-DEC-025**: Document the future considerations for ADR-014 regarding Python version ceiling — ADR-014 documents the 3.8 minimum floor but the `setup.py` classifiers stop at Python 3.10. The ADR does not document a policy for when new Python versions will be added to the classifier list, nor does it document whether the library has been tested on 3.11, 3.12, or 3.13 (all of which are currently active releases). This gap leaves the library's actual compatibility range ambiguous to potential users browsing PyPI.

## Risk Verification
- [x] 🔴 **TODO-RISK-001**: **Verify R-01 mitigation completeness — no `.gitignore` exists in the repository.** The risk document recommends adding a `.gitignore` rule for `*.yaml` / `config.yaml`, but no `.gitignore` file is present anywhere in the repo (`list_files` confirms only 16 files exist, none named `.gitignore`). The TODO is to create `.gitignore` with entries for `*.yaml`, `config.yaml`, `.env`, `*.env`, and `logs/` to prevent accidental secret commits. *(Refs: R-01 · `examples/config.yaml`)*
- [x] 🔴 **TODO-RISK-002**: **Verify R-02 mitigation — `Config.save()` still dumps the full `self.config` dict including `client_secret` with zero filtering.** Confirmed in `avd_manager/config.py` lines 79–83: `yaml.dump(self.config, f, ...)` with no sanitisation. The risk document's proposed `_SENSITIVE_KEYS` exclusion set has not been implemented. Add a unit test asserting `client_secret` is absent from the `save()` output. *(Refs: R-02 · `avd_manager/config.py`)*
- [x] 🔴 **TODO-RISK-003**: **Document and remediate the `.env` file secret-exposure vector not covered by R-01/R-02.** `README.md` lines 28–34 explicitly instruct users to create a `.env` file containing `AZURE_CLIENT_SECRET`. `Config.__init__()` calls `load_dotenv()` unconditionally. If `.env` is committed (no `.gitignore` exists — see TODO-RISK-001), the secret is exposed identically to the YAML path. The risk document does not register this as a separate risk. Add a new risk entry and add `.env` to `.gitignore`. *(Gap — not covered by any existing R-* entry · `README.md` lines 28–34, `avd_manager/config.py` line 22)*
- [x] 🔴 **TODO-RISK-004**: **Verify R-03 mitigation — subscription ID is still logged at `INFO` in `connection_manager.py` line 31.** The risk document recommends downgrading to `DEBUG` or masking, but the code is unchanged: `logger.info(f"Initialized ConnectionManager for subscription: {subscription_id}")`. Confirm the fix is applied and add a test asserting the log level. *(Refs: R-03 · `avd_manager/connection_manager.py` line 31)*
- [x] ⚠️ **TODO-RISK-005**: **Document the `AzureAuthenticator` credential-in-memory exposure risk not addressed in R-04/R-09.** `AzureAuthenticator` stores `client_secret` as a plain instance attribute (`self.client_secret`, `auth.py` line 21) with no memory protection or zeroing on deletion. Any heap dump or debugger attach exposes the secret in plaintext. The risk document does not mention this. Add a new risk entry and recommend using `SecretStr` or clearing the attribute after `ClientSecretCredential` is constructed. *(Gap — not covered by any existing R-* entry · `avd_manager/utils/auth.py` lines 19–22)*
- [x] ⚠️ **TODO-RISK-006**: **Document the `connect()` silent-return-False security gap not fully captured by R-05.** `ConnectionManager.connect()` lines 62–64 catches all exceptions and returns `False` — including `azure.core.exceptions.ClientAuthenticationError`. A caller that ignores the `False` return value will proceed with `self.avd_client = None`, causing a `ConnectionError` only on the next API call. The risk document focuses on data-read methods for R-05 but does not call out the `connect()` method itself. Add this to the R-05 evidence block or register as a new risk. *(Gap — partially covered by R-05 · `avd_manager/connection_manager.py` lines 56–64)*
- [x] 🔴 **TODO-RISK-007**: **Verify R-13 mitigation — `monitor_sessions()` N+1 loop is still present and unparallelised.** Confirmed in `session_handler.py` lines 99–112: sequential `for host in session_hosts` loop with one `get_user_sessions()` call per host, no `ThreadPoolExecutor`, no bulk endpoint. The risk document's recommended `concurrent.futures` fix has not been applied. Document the exact throttle budget impact (51 calls for 50 hosts) and add the parallelisation implementation. *(Refs: R-13 · `avd_manager/session_handler.py` lines 99–112)*
- [x] 🔴 **TODO-RISK-008**: **Document the `compute_client` and `network_client` dead-weight instantiation cost not covered by any risk entry.** `ConnectionManager.connect()` instantiates `ComputeManagementClient` and `NetworkManagementClient` (lines 52–60) on every `connect()` call, but neither client is used anywhere in the codebase (confirmed by full file search — no `self.compute_client` or `self.network_client` method calls exist outside `__init__` and `disconnect()`). Each SDK client instantiation performs HTTP transport setup. This is wasted overhead and dead code. Register as a new risk entry and either remove the unused clients or document their intended future use. *(Gap — not covered by any existing R-* entry · `avd_manager/connection_manager.py` lines 52–60)*
- [x] ⚠️ **TODO-RISK-009**: **Verify R-14 mitigation — no retry or back-off logic has been added to any API call.** All five data-access methods in `session_handler.py` and both list methods in `connection_manager.py` still use bare `try/except Exception` with immediate return on failure. No `tenacity` import exists anywhere in the codebase. The risk document's recommended `tenacity`-based retry has not been implemented. Add implementation evidence or update the roadmap timeline. *(Refs: R-14 · `avd_manager/connection_manager.py`, `avd_manager/session_handler.py`)*
- [x] ⚠️ **TODO-RISK-010**: **Verify R-15 mitigation — all three `list()` calls still eagerly materialise full result sets.** Confirmed: `list(self.avd_client.host_pools.list_by_resource_group(...))` in `connection_manager.py` line 72, `list(self.connection_manager.avd_client.session_hosts.list(...))` in `session_handler.py` line 29, and `list(self.connection_manager.avd_client.user_sessions.list(...))` in `session_handler.py` line 46. No generator wrapper or `max_results` parameter has been added. Document memory ceiling estimates for large deployments. *(Refs: R-15 · `avd_manager/connection_manager.py` line 72, `avd_manager/session_handler.py` lines 29, 46)*
- [x] ⚠️ **TODO-RISK-011**: **Document the `host.name.split('/')[-1]` fragile name-parsing pattern as a latent performance and correctness risk.** `monitor_sessions()` line 104 and `basic_usage.py` line 38 both use `host.name.split('/')[-1]` to extract the bare host name from a fully-qualified ARM resource ID. If the Azure SDK returns a name-only string (no `/`), `split('/')[-1]` still works, but if the format changes across API versions, this silently produces wrong host names, causing `get_user_sessions()` to return empty lists that are indistinguishable from genuine zero-session hosts (compounding R-05). Register as a new medium risk entry. *(Gap — not covered by any existing R-* entry · `avd_manager/session_handler.py` line 104)*
- [x] 🔴 **TODO-RISK-012**: **Verify R-04/R-09 mitigation — `AzureAuthenticator` is still imported but never called in `ConnectionManager`.** Confirmed: `connection_manager.py` line 8 imports `AzureAuthenticator`; `authenticate()` lines 34–41 hardcodes `DefaultAzureCredential()` directly. The service-principal path via `ClientSecretCredential` remains unreachable through the public API. The risk document's recommended refactor (delegating to `authenticator.get_credential()`) has not been implemented. Add a failing integration test to enforce this. *(Refs: R-04, R-09 · `avd_manager/connection_manager.py` lines 8, 34–41)*
- [x] 🔴 **TODO-RISK-013**: **Document the complete test coverage gap — `SessionHandler`, `AzureAuthenticator`, and `Config.save()` have zero test coverage.** Confirmed by `list_files`: only `test_config.py` (5 tests, none covering `save()`) and `test_connection_manager.py` (4 tests) exist. There is no `test_session_handler.py` and no `test_auth.py`. The risk document mentions this in the Executive Summary but does not register it as a formal risk entry (no R-* ID). Create a new risk entry (e.g., R-22) and add test files for all three untested modules. *(Gap — mentioned in Executive Summary but not in Risk Register · `tests/` directory)*
- [x] ⚠️ **TODO-RISK-014**: **Verify R-10 mitigation — `from datetime import datetime` unused import is still present.** Confirmed in `session_handler.py` line 3: `from datetime import datetime` is imported but never referenced in the file. The risk document correctly identifies this (R-10) but the fix has not been applied. Remove the import and add `flake8`/`ruff` to CI to prevent recurrence. *(Refs: R-10 · `avd_manager/session_handler.py` line 3)*
- [x] ⚠️ **TODO-RISK-015**: **Document the `setup.py` classifier gap — Python 3.11 and 3.12 are missing from classifiers despite being current supported versions.** `setup.py` lines 22–26 list classifiers only for Python 3.8, 3.9, and 3.10. Python 3.11 and 3.12 are current stable releases. Combined with R-18 (EOL Python 3.8 minimum), the classifier list is both stale and incomplete, misrepresenting the package's compatibility on PyPI. Update classifiers to reflect the corrected minimum (3.10) and add 3.11 and 3.12. *(Gap — partially related to R-18 · `setup.py` lines 22–26)*
- [x] ⚠️ **TODO-RISK-016**: **Document the `Config.set()` method as an unvalidated arbitrary-key injection vector.** `config.py` lines 62–64 implement `set(key, value)` with no key allowlist, no type checking, and no validation. A caller can inject arbitrary keys (e.g., `config.set('client_secret', 'injected')`) that will then be serialised by `Config.save()` (R-02) or silently override validated fields. The risk document does not register this. Add input validation to `set()` or document the intended usage contract. *(Gap — not covered by any existing R-* entry · `avd_manager/config.py` lines 62–64)*
- [x] 🔴 **TODO-RISK-017**: **Verify R-20 mitigation — no CI/CD pipeline file exists anywhere in the repository.** Confirmed by `list_files`: no `.github/workflows/`, `azure-pipelines.yml`, `tox.ini`, or `pyproject.toml` is present. `CONTRIBUTING.md` line 46 still references "Ensure CI checks pass" — a broken promise with no enforcement mechanism. The risk document's recommended GitHub Actions workflow has not been created. Add `.github/workflows/ci.yml` running `unittest`, `flake8`, `mypy`, and `pip-audit`. *(Refs: R-20 · Repository root)*
- [x] ⚠️ **TODO-RISK-018**: **Verify R-19 mitigation — no `is_connected()` or health-probe method has been added to `ConnectionManager`.** Confirmed: `connection_manager.py` exposes only `authenticate()`, `connect()`, `list_host_pools()`, `get_host_pool()`, and `disconnect()`. There is no `is_connected()`, `ping()`, or token-expiry check. The `disconnect()` method (lines 99–103) still only nulls out references without closing HTTP sessions. Document the token TTL lifecycle and implement the health-probe method. *(Refs: R-19 · `avd_manager/connection_manager.py`)*
- [x] ⚠️ **TODO-RISK-019**: **Verify R-21 mitigation — `setup_file_logging()` still uses a plain `FileHandler` with no rotation.** Confirmed in `logger.py` line 48: `file_handler = logging.FileHandler(log_file)` — no `RotatingFileHandler`, no `max_bytes`, no `backup_count`. The risk document correctly identifies this (R-21) but the fix has not been applied. Replace with `logging.handlers.RotatingFileHandler` and expose `max_bytes` and `backup_count` as parameters. *(Refs: R-21 · `avd_manager/utils/logger.py` line 48)*
- [x] 📝 **TODO-RISK-020**: **Document the missing `docs/` directory content as an operational risk.** `README.md` project structure (line 85) lists a `docs/` directory, but `list_files` confirms no files exist inside it — the directory is either empty or absent entirely. `CONTRIBUTING.md` instructs contributors to "Update README.md if adding new features" and "Add docstrings to new functions/classes" but there is no API reference, architecture guide, or changelog. Register this as a new low/medium operational risk: undocumented public API increases onboarding time and the probability of misuse (e.g., ignoring `False` return values from `connect()` and `disconnect_user_session()`). *(Gap — not covered by any existing R-* entry · Repository root `docs/`)*

## Glossary Verification
- [x] 🔴 **TODO-GLOSS-001**: Add missing term `ConnectionError` — This Python built-in exception is **raised** (not just returned) in four distinct guard clauses across `session_handler.py` (lines 25, 44, 63, 81) and `connection_manager.py` (line 45) whenever a method is called before `connect()` has been invoked. The glossary documents `False`/`None` return patterns but never explains the `ConnectionError` raise path, leaving readers unaware of when exceptions vs. return values should be expected.
- [x] 🔴 **TODO-GLOSS-002**: Add missing term `sessions` (Status Dictionary key) — The `"sessions"` key in the Status Dictionary maps to `session_host.sessions`, an integer count of currently active sessions on a host. The glossary defines the Status Dictionary and its other four keys (`"name"`, `"status"`, `"last_heartbeat"`, `"update_state"`) individually, but `"sessions"` is never defined as a standalone term despite being the primary capacity metric for a session host.
- [x] 🔴 **TODO-GLOSS-003**: Add missing term `utils` subpackage — The `avd_manager/utils/` directory is a distinct importable sub-package with its own `__init__.py` and `__all__` that exports `get_logger` and `AzureAuthenticator`. It is referenced throughout the glossary (e.g., under `AzureAuthenticator`, `get_logger`) but is never defined as a term in its own right, leaving its role as the internal utility layer undocumented.
- [x] ⚠️ **TODO-GLOSS-004**: Add missing term `.env` file — `README.md` explicitly instructs users to create a `.env` file in the project root as the primary configuration mechanism, and `Config.__init__()` calls `load_dotenv()` unconditionally on every instantiation to load it. The glossary covers `python-dotenv` as a dependency and `load_dotenv()` in passing, but never defines what a `.env` file is, its expected location, or its relationship to the five recognized environment variables.
- [x] ⚠️ **TODO-GLOSS-005**: Add missing term `datetime` import — `session_handler.py` imports `from datetime import datetime` at the module level (line 2), yet `datetime` is never used anywhere in the current source. The glossary should note this unused import as a placeholder for future timestamp-based session tracking logic, clarifying its intent so contributors do not remove it as dead code.
- [x] ⚠️ **TODO-GLOSS-006**: Add missing term `Resource Path Splitting` — The pattern `host.name.split('/')[-1]` appears in both `basic_usage.py` (line 40) and `session_handler.py` (line 106) to extract the short name from a full Azure ARM resource path (e.g., `/subscriptions/.../hostPools/my-pool`). This is a recurring, non-obvious convention that is mentioned in the `session_host_name` and `host_pool_name` entries but never named or explained as a standalone pattern.
- [x] 🔴 **TODO-GLOSS-007**: Expand `PEP 8` acronym — `CONTRIBUTING.md` (line 26) mandates "Follow PEP 8 guidelines" as a code-style requirement, but `PEP` is never expanded or defined in the Abbreviations section. Add an entry for **PEP** (Python Enhancement Proposal) with a note that PEP 8 is the style guide for Python code, as it is the only style standard referenced for contributors.
- [x] 🔴 **TODO-GLOSS-008**: Add missing term `__all__` — Both `avd_manager/__init__.py` (line 10) and `avd_manager/utils/__init__.py` (line 6) define `__all__` to declare the public API surface. The glossary describes what each package exports but never explains `__all__` itself as the Python mechanism that controls `from package import *` behavior and signals the intended public interface.
- [x] 🔴 **TODO-GLOSS-009**: Add missing term `__version__` — `avd_manager/__init__.py` (line 3) declares `__version__ = "0.1.0"` as a module-level dunder attribute. The glossary mentions version `0.1.0` in its header but never documents `__version__` as the canonical programmatic version accessor, nor explains that it is the single source of truth for the package version (distinct from `setup.py`'s `version=` argument).
- [x] ⚠️ **TODO-GLOSS-010**: Clarify `AAD` / Azure Active Directory — The glossary uses "Azure Active Directory" in the `Service Principal` and `tenant_id` entries but never abbreviates or defines `AAD`. Microsoft has rebranded Azure Active Directory to **Microsoft Entra ID**; the glossary should acknowledge both names and add `AAD` to the Abbreviations section to prevent confusion when readers encounter either name in Azure portal documentation.
- [x] ⚠️ **TODO-GLOSS-011**: Add missing term `ARM` (Azure Resource Manager) — Azure resource paths (e.g., `/subscriptions/{id}/resourceGroups/{rg}/providers/...`) follow the ARM path convention. The glossary references these paths in `session_host_name` and `host_pool_name` entries without ever defining ARM or explaining the path structure, leaving readers unable to interpret the raw strings returned by the SDK.
- [x] 📝 **TODO-GLOSS-012**: Add missing abbreviation `VM` — The glossary uses "virtual machine" and "VM" interchangeably in the `Session Host`, `Resource Group`, and `compute_client` entries without ever formally defining `VM` in the Abbreviations section. Add a concise entry cross-referencing `Session Host` and `ComputeManagementClient`.
- [x] 🔴 **TODO-GLOSS-013**: Document `AzureAuthenticator` vs. `ConnectionManager.authenticate()` divergence — The glossary defines both but does not explain that they are **parallel, non-integrated** authentication paths. `ConnectionManager.authenticate()` directly instantiates `DefaultAzureCredential` without using `AzureAuthenticator`, even though `AzureAuthenticator` is imported in `connection_manager.py` (line 8) and never called. This architectural inconsistency is a critical gap for contributors deciding which path to extend.
- [x] 🔴 **TODO-GLOSS-014**: Add missing term `setUp()` test fixture method — Both `TestConfig` and `TestConnectionManager` use `setUp()` to initialize shared test state before each test method. The glossary documents both test classes but never defines `setUp()` as the `unittest.TestCase` lifecycle hook responsible for creating fresh fixtures, which is essential context for contributors writing new tests.
- [x] 🔴 **TODO-GLOSS-015**: Clarify incomplete definition of `active_sessions` — The glossary defines `active_sessions` as "an empty `{}` at construction time" and "intended as an in-memory store," but never documents that it is **never written to** by any current method in `SessionHandler`. `monitor_sessions()` builds a local `all_sessions` list instead of populating `self.active_sessions`. The definition should flag this as a stub/placeholder for future stateful session caching.
- [x] ⚠️ **TODO-GLOSS-016**: Add missing term `list_by_resource_group()` SDK method — The glossary defines the `host_pools` SDK sub-client but only mentions `list_by_resource_group()` inline within `list_host_pools()`. This is the specific Azure SDK pagination method that returns an iterable (not a plain list), which is why `ConnectionManager.list_host_pools()` wraps it in `list(...)`. The wrapping behavior and its implications for large environments deserve a dedicated entry.
- [x] ⚠️ **TODO-GLOSS-017**: Add missing term `PEP 8` / `python_requires` constraint — `setup.py` declares `python_requires=">=3.8"` and lists classifiers for Python 3.8, 3.9, and 3.10. The glossary mentions `setup.py` only for the PyPI distribution name and dependency list. The supported Python version range is a key compatibility term that should be documented, especially since f-strings and `pathlib` usage in the codebase depend on Python 3.6+ features.
- [x] 📝 **TODO-GLOSS-018**: Add missing term `Mock` / `patch` testing utilities — The test files use `unittest.mock.Mock`, `unittest.mock.patch`, and `patch.dict` extensively to isolate Azure SDK calls. The glossary documents `TestConfig` and `TestConnectionManager` as entity names but never defines the mocking strategy itself. A brief entry on the `unittest.mock` pattern used (patching `DefaultAzureCredential` at the import site) would help contributors write consistent new tests.

## Dataflow Verification
- [x] 🔴 **TODO-FLOW-001**: Document the full `HostPool` SDK model schema — the doc only references `.name`, `.host_pool_type`, and `.load_balancer_type` (seen in `basic_usage.py:43-44`), but the ARM response object contains many additional fields (e.g., `description`, `friendly_name`, `max_session_limit`, `registration_info`, `vm_template`) that are silently discarded; enumerate all fields returned by `avd_client.host_pools.get()` and note which are used vs. ignored.
- [x] 🔴 **TODO-FLOW-002**: Document the full `SessionHost` SDK model schema — `session_handler.py` only reads `.name` (line 101 in `monitor_sessions`) and the five fields extracted in `get_session_host_status()` (`name`, `status`, `sessions`, `last_heart_beat`, `update_state`); all other `SessionHost` fields are undocumented and silently dropped.
- [x] 🔴 **TODO-FLOW-003**: Document the full `UserSession` SDK model schema — `get_user_sessions()` (`session_handler.py:44`) returns raw `UserSession` SDK objects to the caller but the doc provides no field inventory; fields such as `user_principal_name`, `session_id`, `session_state`, `application_type` are never described.
- [x] ⚠️ **TODO-FLOW-004**: Document the `host_pools` list schema from `config.yaml` — the YAML key `host_pools` (with sub-keys `name` and `region`, `examples/config.yaml:18-22`) is listed in the YAML key table in §2.2 but its sub-structure (`name`, `region`) is never described, and no code in the library reads this key at runtime; clarify whether it is purely informational or reserved for future use.
- [x] ⚠️ **TODO-FLOW-005**: Document the `logging` nested YAML key schema — `examples/config.yaml:13-14` defines `logging.level` and `logging.file` as sub-keys, but §2.2 lists them as flat dotted strings (`logging.level`, `logging.file`) rather than a nested dict; clarify the actual parsed structure after `yaml.safe_load()` produces `{'logging': {'level': ..., 'file': ...}}` vs. the flat representation shown in the table.
- [x] ⚠️ **TODO-FLOW-006**: Document the `get_session_host_status()` output dict schema explicitly in §6.3 — the five keys (`name`, `status`, `sessions`, `last_heartbeat`, `update_state`) are shown in the flow diagram but their value types (e.g., `status` is a string enum, `sessions` is an int, `last_heartbeat` is a `datetime`) are never specified; note the field name mismatch between the SDK attribute `last_heart_beat` and the dict key `last_heartbeat`.
- [x] ⚠️ **TODO-FLOW-007**: Document the `AzureAuthenticator.__init__()` data flow — §11.4 lists the four instance attributes but the doc never shows the initialization flow diagram (analogous to §2.1 for `Config`) showing how `tenant_id`, `client_id`, `client_secret` flow from the caller into the object; add a flow block for `auth.py:11-23`.
- [x] 📝 **TODO-FLOW-008**: Clarify the `Config.config` dict structure after `_load_config()` completes — the doc describes the two-stage merge in §2.2 but never shows the final shape of `self.config` as a concrete dict example (e.g., `{'subscription_id': '...', 'resource_group': '...', 'tenant_id': None, ..., 'logging': {'level': 'INFO', 'file': '...'}, 'host_pools': [...]}`).
- [x] 📝 **TODO-FLOW-009**: Document the `UserSession` `session_id` field type — `disconnect_user_session()` (`session_handler.py:57`) accepts `session_id` as a parameter and passes it directly to the ARM SDK, but the doc never specifies whether this is an integer, a string, or a GUID, which is critical for callers extracting it from `UserSession` objects.
- [x] 📝 **TODO-FLOW-010**: Document the `SessionHost.name` ARM path format — §7 notes that `host.name.split('/')[-1]` is used to strip the ARM resource path prefix, but the full path format (e.g., `hostPools/{poolName}/sessionHosts/{hostName}`) is never shown; add the expected full string format to clarify why the split is necessary.
- [x] 🔴 **TODO-FLOW-011**: The ARM endpoint for `session_hosts.list()` in §6.1 is documented, but the equivalent ARM endpoint for `session_hosts.get()` used in `get_session_host_status()` (`session_handler.py:76`) is missing from §6.3 — add `GET /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.DesktopVirtualization/hostPools/{hostPoolName}/sessionHosts/{sessionHostName}`.
- [x] 🔴 **TODO-FLOW-012**: Document the OAuth 2.0 token endpoint URL used by `DefaultAzureCredential` — §10.1 describes the Azure AD token flow but never specifies the actual endpoint (`https://login.microsoftonline.com/{tenantId}/oauth2/v2.0/token`); this is especially relevant for the `DefaultAzureCredential` path where `tenant_id` may not be known at call time.
- [x] 🔴 **TODO-FLOW-013**: Document the ARM API version used by `azure-mgmt-desktopvirtualization==1.0.0` — all six ARM endpoint entries in §10.2 omit the `api-version` query parameter that the SDK appends to every request; this is critical for reproducibility and debugging.
- [x] ⚠️ **TODO-FLOW-014**: Document the ARM base URL used by the SDK clients — all ARM endpoints in §5, §6, and §8 are shown as relative paths but the base URL (`https://management.azure.com`) is never stated; add it to §10 or the endpoint tables.
- [x] ⚠️ **TODO-FLOW-015**: Document the HTTP response codes and response body shapes for each ARM call — §5.1, §5.2, §6.1, §6.2, §6.3, and §8 describe what the SDK returns but never document the raw HTTP status codes (200, 204 for disconnect, 404 for not-found) that the SDK translates into exceptions or return values.
- [x] ⚠️ **TODO-FLOW-016**: Document the ARM pagination behavior for `list_by_resource_group()` and `session_hosts.list()` — both methods call `list(...)` to materialise a lazy iterator (`connection_manager.py:70`, `session_handler.py:27`), but the doc does not explain that the SDK transparently handles `nextLink` pagination; add a note on how many items per page the API returns and whether there is a practical limit.
- [x] ⚠️ **TODO-FLOW-017**: Document the `user_sessions.disconnect()` ARM call response — §8 states the return is `None (void ARM call)` but does not document the HTTP 200/204 response the ARM API returns, nor whether the SDK raises on non-2xx; this is the only write operation in the library and deserves fuller treatment.
- [x] ⚠️ **TODO-FLOW-018**: Document the ARM scope/permission requirements for each endpoint — §10.2 lists the operations but never specifies the required Azure RBAC roles (e.g., `Desktop Virtualization Reader` for GET operations, `Desktop Virtualization Session Operator` for the disconnect POST); this is a critical operational gap.
- [x] 📝 **TODO-FLOW-019**: Verify and document the `HostPool.name` field format returned by `list_by_resource_group()` — §5.2 notes the caller uses `.name.split('/')[-1]` but §5.1 does not mention that `list_by_resource_group()` also returns fully-qualified names; confirm whether both endpoints return the same format or if only `get()` returns the qualified path.
- [x] 📝 **TODO-FLOW-020**: Document the `ComputeManagementClient` and `NetworkManagementClient` ARM base endpoints and API versions — §10.3 and §10.4 note these clients are instantiated but unused; for completeness, document the base URLs they would target (`https://management.azure.com`, compute API version, network API version) so future implementers have the reference.
- [x] 🔴 **TODO-FLOW-021**: Document the `datetime` import in `session_handler.py:1` — `from datetime import datetime` is imported at the top of `session_handler.py` but is never used anywhere in the file; the dataflow doc does not mention this dead import, which may indicate a planned but unimplemented timestamp transformation (e.g., converting `last_heart_beat` to a formatted string); document its intended purpose or flag it as a dead import.
- [x] 🔴 **TODO-FLOW-022**: Document the `AzureAuthenticator` import in `connection_manager.py:8` — `from .utils.auth import AzureAuthenticator` is imported at the top of `connection_manager.py` but `AzureAuthenticator` is never instantiated or called anywhere in that file; §3.2 notes this but the import itself is not flagged in the flow; add a note in §3 or §10 that this is a dead import creating a misleading dependency signal.
- [x] 🔴 **TODO-FLOW-023**: Document the `Config.save()` data transformation in detail — §2.5 shows the flow but does not describe what happens when `self.config` contains `None` values (from env vars that were not set); `yaml.dump()` will serialize `None` as `null`, which may produce an invalid config file if reloaded; document this edge case.
- [x] ⚠️ **TODO-FLOW-024**: Document the `_load_config()` behavior when `yaml.safe_load()` returns `None` — if the YAML file is empty, `yaml.safe_load()` returns `None` rather than `{}`, which would cause `self.config.get(...)` calls in the env-var overlay stage to raise `AttributeError`; this edge case is not documented in §2.2 and represents a latent bug.
- [x] ⚠️ **TODO-FLOW-025**: Document the data flow for the `logging.level` YAML key — §2.2 lists `logging.level` as a supported YAML key but no code in the library reads `config.get('logging')` and passes the level to `get_logger()`; the `get_logger()` function accepts a `level` parameter (`logger.py:8`) but is always called with the default `logging.INFO`; document this gap between the config schema and actual runtime behavior.
- [x] ⚠️ **TODO-FLOW-026**: Document the data flow for the `logging.file` YAML key — similarly, `logging.file` is listed in the YAML schema (§2.2) and `examples/config.yaml:14` sets it to `"logs/avd_manager.log"`, but no library code reads this key and calls `setup_file_logging()`; §9.2 mentions this but §2.2 does not cross-reference the gap; add a cross-reference note in the YAML key table.
- [x] ⚠️ **TODO-FLOW-027**: Document the `monitor_sessions()` error propagation gap — if `list_session_hosts()` returns `[]` due to a caught exception (Pattern A), `monitor_sessions()` silently returns `[]` with no indication of the upstream failure; but if `list_session_hosts()` raises `ConnectionError` (Pattern B), it propagates unhandled through `monitor_sessions()`; this asymmetric behavior is not documented in §7 or §12.
- [x] ⚠️ **TODO-FLOW-028**: Document the `connect()` partial-failure scenario — if `DesktopVirtualizationMgmtClient` instantiation succeeds but `ComputeManagementClient` raises an exception (`connection_manager.py:52-62`), `avd_client` will be set but `compute_client` and `network_client` will remain `None`, and `connect()` returns `False`; this partial state is not documented in §4 or §11.1.
- [x] 📝 **TODO-FLOW-029**: Document the `Config.set()` log level discrepancy — §2.4 states `set()` "logs at DEBUG level" (`config.py:60`), but `get_logger()` sets the default level to `INFO` (`logger.py:20`), meaning DEBUG messages from `set()` are silently suppressed unless the caller explicitly lowers the log level; document this in §9 as a known logging gap.
- [x] 📝 **TODO-FLOW-030**: Document the `HostPool` object field access pattern in `basic_usage.py` — §14 shows `pool_details.host_pool_type` and `pool_details.load_balancer_type` being accessed directly on the raw SDK object, but these are not documented as part of any data model schema; cross-reference with the `HostPool` schema TODO (FLOW-001) and note that raw SDK objects are returned, not dicts.
- [x] 🔴 **TODO-FLOW-031**: Document the `active_sessions` dict intended schema — `SessionHandler.active_sessions` is initialized as `{}` (`session_handler.py:17`) and is never written or read by any method; the doc notes it is a placeholder (§11.2) but does not document its intended key/value schema (e.g., `{session_id: UserSession}` or `{host_name: List[UserSession]}`); without this, future implementers have no contract to follow.
- [x] 🔴 **TODO-FLOW-032**: Document the `ConnectionManager` re-connect flow — §11.1 shows `credential` is set by `authenticate()` and cleared by `disconnect()`, but there is no documentation of what happens if `connect()` is called a second time on an already-connected manager; `avd_client` would be overwritten silently; document this idempotency (or lack thereof) in §4.
- [x] ⚠️ **TODO-FLOW-033**: Document the `AzureAuthenticator.credential` state lifecycle — §11.4 shows `credential` is set by `get_default_credential()` or `get_service_principal_credential()`, but there is no `disconnect()` or `clear()` method on `AzureAuthenticator`; once set, the credential object persists for the lifetime of the `AzureAuthenticator` instance; document this in §11.4 and contrast with `ConnectionManager.disconnect()` which does clear its credential.
- [x] ⚠️ **TODO-FLOW-034**: Document the `Config.config_file` attribute immutability — §11.3 notes `config_file` is "Never" modified after `__init__`, but `_load_config()` can be called again (e.g., if a subclass calls it), which would re-read the same file; document whether `_load_config()` is intended to be called only once and whether calling it multiple times is safe.
- [x] ⚠️ **TODO-FLOW-035**: Document the `ConnectionManager.__init__()` log message data flow — `connection_manager.py:30` logs `f"Initialized ConnectionManager for subscription: {subscription_id}"`, which means the `subscription_id` value is written to `stdout` at initialization; this is a potential credential-adjacent information disclosure (subscription IDs are not secrets but are sensitive); document this in §9.3.
- [x] ⚠️ **TODO-FLOW-036**: Document the `setup_file_logging()` root logger side effect — `logger.py:57` attaches the `FileHandler` to the **root logger** (`logging.getLogger()` with no name), not to a named logger; this means file logging will capture log output from ALL Python modules in the process (including Azure SDK internals), not just `avd_manager` modules; document this behavior in §9.2.
- [x] 📝 **TODO-FLOW-037**: Document the `get_logger()` idempotency guarantee scope — §9.1 notes the `if not logger.handlers` guard prevents duplicate handlers, but this guard only works within a single Python process; if the same module is imported in multiple threads simultaneously, there is a race condition; document the single-threaded assumption (already stated in §1) as the reason this guard is sufficient.
- [x] 📝 **TODO-FLOW-038**: Document the `Config` object's relationship to `ConnectionManager` — the `ConnectionManager` constructor accepts `subscription_id` and `resource_group` as plain strings, not a `Config` object; once constructed, `ConnectionManager` has no reference back to `Config`; document this decoupling explicitly in §11 and note that changes to `Config` after `ConnectionManager` is constructed have no effect on the manager.
- [x] 🔴 **TODO-FLOW-039**: Document the `authenticate()` → `connect()` error propagation chain — §4 shows that if `authenticate()` returns `False`, `connect()` raises `ConnectionError("Failed to authenticate")`; but §12 Pattern A lists `connect()` as returning `False` on exception, while the `ConnectionError` raise at `connection_manager.py:45` is Pattern B; the doc does not reconcile that `connect()` uses **both** patterns depending on the failure point; clarify in §12.
- [x] ⚠️ **TODO-FLOW-040**: Document the `get_default_credential()` error propagation in `AzureAuthenticator` — unlike `ConnectionManager.authenticate()` which catches exceptions and returns `False` (Pattern A), `AzureAuthenticator.get_default_credential()` (`auth.py:36`) catches the exception, logs it, and then **re-raises** it; this is a third error pattern not documented in §12; add it as Pattern C.
- [x] ⚠️ **TODO-FLOW-041**: Document the `get_service_principal_credential()` dual-exception behavior — `auth.py:46` raises `ValueError` for missing credentials (a pre-condition guard, similar to Pattern B) and `auth.py:55` re-raises SDK exceptions (similar to the undocumented Pattern C); document both branches in §12 for completeness.
- [x] ⚠️ **TODO-FLOW-042**: Document what exception type is caught by the bare `except Exception` blocks — all Pattern A methods catch `Exception` broadly; document whether Azure SDK methods raise `azure.core.exceptions.HttpResponseError`, `azure.core.exceptions.ResourceNotFoundError`, or other specific subtypes, and note that the current broad catch means HTTP 403 (permission denied) and HTTP 404 (not found) are both silently swallowed and return the same sentinel value.
- [x] 📝 **TODO-FLOW-043**: Document the `monitor_sessions()` missing try/except — unlike every other method in `session_handler.py`, `monitor_sessions()` (`session_handler.py:95-107`) has **no try/except block**; if `get_user_sessions()` raises `ConnectionError` (Pattern B) mid-loop, the partial `all_sessions` list is discarded and the exception propagates to the caller; document this inconsistency in §7 and §12.
- [x] 🔴 **TODO-FLOW-044**: Document the `requests==2.31.0` dependency data flow — §13.3 correctly notes that `requests` is declared in both `requirements.txt:7` and `setup.py:17` but is never imported anywhere in the library source; the doc should explicitly flag this as a **dead dependency** that should either be removed or have its intended use case documented (e.g., was it intended for direct ARM REST calls before the SDK was adopted?).
- [x] 🔴 **TODO-FLOW-045**: Document the `setup.py` vs `requirements.txt` version pinning discrepancy — `requirements.txt` uses exact pins (e.g., `azure-identity==1.15.0`) while `setup.py` uses minimum-version constraints (e.g., `azure-identity>=1.15.0`); the dataflow doc references `azure-identity==1.15.0` in §10.1 but this is only the pinned dev/test version; document which file governs production installs and whether the `>=` bounds in `setup.py` could allow incompatible SDK versions.
- [x] ⚠️ **TODO-FLOW-046**: Document the `python-dotenv` `load_dotenv()` search behavior — §2.1 states `load_dotenv()` "reads .env file from CWD" (`config.py:21`), but `python-dotenv` actually searches upward from CWD through parent directories until it finds a `.env` file; document the actual search path behavior and its implications for library consumers who may have a `.env` in a parent directory.
- [x] ⚠️ **TODO-FLOW-047**: Document the `setup.py` Python version classifiers vs. `python_requires` — `setup.py:20-24` lists classifiers for Python 3.8, 3.9, and 3.10 but `python_requires=">=3.8"` would also allow 3.11 and 3.12; the dataflow doc states "Python 3.8+" in §1 but does not note this classifier/requirement gap; document the tested Python versions.
- [x] ⚠️ **TODO-FLOW-048**: Document the `AzureAuthenticator` public API export path — `utils/__init__.py:4` exports `AzureAuthenticator` via `from .auth import AzureAuthenticator`, and `avd_manager/__init__.py` does **not** re-export it (only `ConnectionManager`, `SessionHandler`, `Config` are in `__all__`); callers must import it as `from avd_manager.utils import AzureAuthenticator`; document this import path in §3.1 since the doc implies it is available for direct use.
- [x] 📝 **TODO-FLOW-049**: Document the `DefaultAzureCredential` credential chain order — §3.1 Path B states it "tries: env vars → managed identity → CLI → etc." but the actual `DefaultAzureCredential` chain in `azure-identity==1.15.0` has a specific order (EnvironmentCredential → WorkloadIdentityCredential → ManagedIdentityCredential → SharedTokenCacheCredential → VisualStudioCodeCredential → AzureCliCredential → AzurePowerShellCredential); document the full chain so operators know which credential source will be used in each deployment environment.
- [x] 📝 **TODO-FLOW-050**: Document the token refresh behavior during long-running sessions — §15 notes that `azure-identity` internally caches the OAuth 2.0 bearer token, but does not document what happens when the token expires mid-operation (e.g., during a `monitor_sessions()` call that makes many ARM requests); document whether `DefaultAzureCredential` and `ClientSecretCredential` automatically refresh expired tokens transparently, or whether callers need to handle `401 Unauthorized` responses.

## Code Verification
- [x] 🔴 **TODO-CODE-001**: Verify `Config.__init__()` docstring — the `Args:` section only documents `config_file` but does not mention the side-effect of calling `load_dotenv()` at construction time; confirm this is intentional and add a `Note:` to the documentation at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:14)
- [x] 🔴 **TODO-CODE-002**: Document the `Config._load_config()` method's behaviour when `yaml.safe_load()` returns `None` (e.g., empty YAML file) — `self.config` would be set to `None`, causing `AttributeError` on subsequent `.get()` calls; this edge case is undocumented at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:27)
- [x] 🔴 **TODO-CODE-003**: Add a `Returns:` section to the `Config._load_config()` docstring — the documentation table in Section 5 lists it as returning `None` but the actual docstring at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:27) has no `Returns:` entry
- [x] 🔴 **TODO-CODE-004**: Document the `Config.get()` method's full docstring — the current source at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:62) only has a one-line docstring with no `Args:` or `Returns:` sections, contradicting the Google-style docstring convention stated in Section 6
- [x] 🔴 **TODO-CODE-005**: Document the `Config.set()` method's full docstring — the source at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:66) only has a one-line docstring with no `Args:` section, violating the stated Google-style convention
- [x] 🔴 **TODO-CODE-006**: Document the `Config.validate()` method's `Raises:` section — the docstring at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:70) has no `Raises:` entry despite raising `ValueError`, which is a critical omission for callers
- [x] 🔴 **TODO-CODE-007**: Document the `Config.save()` method's full docstring — the source at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:79) has only a one-line docstring with no `Args:` section documenting the `output_file` parameter
- [x] 🔴 **TODO-CODE-008**: Verify `ConnectionManager.authenticate()` docstring — the source at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:33) has only a one-line docstring with no `Returns:` section, despite the method returning a `bool`
- [x] 🔴 **TODO-CODE-009**: Verify `ConnectionManager.connect()` docstring — the source at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:42) has only a one-line docstring with no `Returns:` or `Raises:` sections, despite raising `ConnectionError` and returning `bool`
- [x] 🔴 **TODO-CODE-010**: Document `ConnectionManager.list_host_pools()` full docstring — the source at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:64) has only a one-line docstring with no `Returns:` or `Raises:` sections
- [x] 🔴 **TODO-CODE-011**: Document `ConnectionManager.get_host_pool()` full docstring — the source at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:77) has only a one-line docstring with no `Args:`, `Returns:`, or `Raises:` sections
- [x] 🔴 **TODO-CODE-012**: Document `ConnectionManager.disconnect()` full docstring — the source at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:92) has only a one-line docstring with no description of which attributes are nulled out
- [x] ⚠️ **TODO-CODE-013**: Verify `SessionHandler.list_session_hosts()` docstring completeness — the source at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:23) has only a one-line docstring; add `Args:`, `Returns:`, and `Raises:` sections
- [x] ⚠️ **TODO-CODE-014**: Verify `SessionHandler.get_user_sessions()` docstring completeness — the source at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:40) has only a one-line docstring; add `Args:`, `Returns:`, and `Raises:` sections
- [x] ⚠️ **TODO-CODE-015**: Verify `SessionHandler.disconnect_user_session()` docstring completeness — the source at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:58) has only a one-line docstring; add `Args:`, `Returns:`, and `Raises:` sections
- [x] ⚠️ **TODO-CODE-016**: Verify `SessionHandler.get_session_host_status()` docstring completeness — the source at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:76) has only a one-line docstring; add `Args:`, `Returns:`, and `Raises:` sections
- [x] ⚠️ **TODO-CODE-017**: Verify `SessionHandler.monitor_sessions()` docstring completeness — the source at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:100) has only a one-line docstring; add `Args:` and `Returns:` sections
- [x] ⚠️ **TODO-CODE-018**: Document `AzureAuthenticator.get_default_credential()` `Raises:` section — the source at [`avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py:26) re-raises exceptions but the docstring has no `Raises:` entry
- [x] ⚠️ **TODO-CODE-019**: Document `AzureAuthenticator.get_service_principal_credential()` `Raises:` section — the source at [`avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py:40) raises both `ValueError` and re-raises SDK exceptions, but the docstring has no `Raises:` entry
- [x] ⚠️ **TODO-CODE-020**: Document `AzureAuthenticator.get_credential()` `Raises:` section — the source at [`avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py:60) can propagate exceptions from both delegate methods, but the docstring has no `Raises:` entry
- [x] 📝 **TODO-CODE-021**: Document the `datetime` import in `session_handler.py` — `from datetime import datetime` is present at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:3) but `datetime` is never used anywhere in the file; this unused import is not mentioned in the documentation and should be flagged
- [x] 📝 **TODO-CODE-022**: Add a `Returns:` description to `setup_file_logging()` — the docstring at [`avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py:38) has no `Returns:` section; confirm it returns `None` and document accordingly
- [x] 🔴 **TODO-CODE-023**: Verify the `AzureAuthenticator` internal `credential` attribute — the class stores the last-obtained credential in `self.credential` at [`avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py:22), but this attribute is completely absent from the "Internal state" table in Section 3.4 of the documentation
- [x] 🔴 **TODO-CODE-024**: Clarify the `AzureAuthenticator` vs `ConnectionManager` integration gap — the documentation in Section 3.4 notes that `ConnectionManager` imports `AzureAuthenticator` but does not use it; verify this is accurate against [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:8) and document whether the import is dead code or reserved for future use
- [x] 🔴 **TODO-CODE-025**: Document the `utils` sub-package's `setup_file_logging` omission from `__all__` — `setup_file_logging` is exported by `utils/logger.py` but is NOT re-exported in [`avd_manager/utils/__init__.py`](QA-Test-Repo/avd_manager/utils/__init__.py:1); the documentation does not mention this discrepancy
- [x] 🔴 **TODO-CODE-026**: Verify the `__init__.py` package-level `__all__` does not expose `AzureAuthenticator` or `get_logger` — the top-level [`avd_manager/__init__.py`](QA-Test-Repo/avd_manager/__init__.py:1) only exports `ConnectionManager`, `SessionHandler`, `Config`; document that `AzureAuthenticator` must be imported from `avd_manager.utils` explicitly
- [x] 🔴 **TODO-CODE-027**: Document the `SessionHandler.active_sessions` attribute usage — the attribute is initialised as an empty `dict` at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:19) but is never written to by any method in the class; the documentation states it is "populated externally" — verify and clarify this design decision
- [x] 🔴 **TODO-CODE-028**: Verify the `test_initialization` test coverage for `compute_client` and `network_client` — the test at [`tests/test_connection_manager.py`](QA-Test-Repo/tests/test_connection_manager.py:17) only asserts `credential` and `avd_client` are `None` on init; `compute_client` and `network_client` are not asserted, creating a gap between the documented internal state table and test coverage
- [x] 🔴 **TODO-CODE-029**: Document the `test_disconnect` test gap — the test at [`tests/test_connection_manager.py`](QA-Test-Repo/tests/test_connection_manager.py:40) only sets and checks `credential` and `avd_client`; it does not verify that `compute_client` and `network_client` are also set to `None` after `disconnect()`, which is documented behaviour in Section 5
- [x] ⚠️ **TODO-CODE-030**: Add a missing test class for `SessionHandler` — there is no `tests/test_session_handler.py` file in the repository; all five `SessionHandler` methods (`list_session_hosts`, `get_user_sessions`, `disconnect_user_session`, `get_session_host_status`, `monitor_sessions`) are completely untested
- [x] ⚠️ **TODO-CODE-031**: Add a missing test class for `AzureAuthenticator` — there is no `tests/test_auth.py` file; `get_default_credential()`, `get_service_principal_credential()`, and `get_credential()` are completely untested
- [x] ⚠️ **TODO-CODE-032**: Add a missing test class for `get_logger()` and `setup_file_logging()` — there is no `tests/test_logger.py` file; the duplicate-handler guard in `get_logger()` and the directory-creation logic in `setup_file_logging()` are untested
- [x] ⚠️ **TODO-CODE-033**: Document the `Config.setUp()` fixture's `self.test_config` dict — the dict is created in [`tests/test_config.py`](QA-Test-Repo/tests/test_config.py:13) but is never actually used by any of the five test methods; document whether this is intentional scaffolding or a test gap
- [x] ⚠️ **TODO-CODE-034**: Verify the `Config.save()` method has no test coverage — no test in [`tests/test_config.py`](QA-Test-Repo/tests/test_config.py:1) exercises `Config.save()`; add a TODO to document this gap and create a test using `mock_open` (which is already imported but unused)
- [x] ⚠️ **TODO-CODE-035**: Verify the `Config._load_config()` YAML file-loading path has no test coverage — no test in [`tests/test_config.py`](QA-Test-Repo/tests/test_config.py:1) exercises the YAML file-loading branch; `mock_open` is imported but unused
- [x] ⚠️ **TODO-CODE-036**: Document the `ConnectionManager.connect()` test gap — there is no `test_connect_success` or `test_connect_failure` test in [`tests/test_connection_manager.py`](QA-Test-Repo/tests/test_connection_manager.py:1); the three SDK client instantiations in `connect()` are completely untested
- [x] ⚠️ **TODO-CODE-037**: Document the `ConnectionManager.list_host_pools()` and `get_host_pool()` test gaps — neither method has any test coverage in [`tests/test_connection_manager.py`](QA-Test-Repo/tests/test_connection_manager.py:1)
- [x] ⚠️ **TODO-CODE-038**: Verify the `setup.py` classifiers list — [`setup.py`](QA-Test-Repo/setup.py:20) declares support for Python 3.8, 3.9, and 3.10 but the documentation only states `>= 3.8`; confirm whether 3.11 and 3.12 are also supported and update both `setup.py` and the documentation
- [x] ⚠️ **TODO-CODE-039**: Verify the `setup.py` vs `requirements.txt` version constraint discrepancy — `setup.py` uses `>=` minimum bounds while `requirements.txt` pins exact versions; the documentation in Section 10 notes this but does not explain the implications for library consumers who install via `pip install avd-manager`
- [x] ⚠️ **TODO-CODE-040**: Document the `requests` package usage gap — `requests==2.31.0` is listed in [`requirements.txt`](QA-Test-Repo/requirements.txt:7) and `setup.py` but is not imported anywhere in the source code; the documentation in Section 10 notes it is "not directly imported in current source" but does not explain why it is a declared dependency
- [x] 📝 **TODO-CODE-041**: Document the `docs/` directory referenced in `README.md` — the project structure in [`README.md`](QA-Test-Repo/README.md:87) lists a `docs/` directory but no such directory or files exist in the repository; this should be flagged as a discrepancy
- [x] 📝 **TODO-CODE-042**: Verify the `CONTRIBUTING.md` line number references — the documentation cites `CONTRIBUTING.md` line 26 for PEP 8 and line 28 for docstrings, but the actual file at [`CONTRIBUTING.md`](QA-Test-Repo/CONTRIBUTING.md:1) has PEP 8 at line 26 and docstrings at line 28 under "Code Style" — confirm these are accurate
- [x] 📝 **TODO-CODE-043**: Document the `mock_open` import in `test_config.py` — `mock_open` is imported at [`tests/test_config.py`](QA-Test-Repo/tests/test_config.py:5) but is never used in any test method; document whether this is a planned-but-missing test or dead import
- [x] 📝 **TODO-CODE-044**: Verify the `__version__` and `__author__` attributes are not re-exported in `__all__` — [`avd_manager/__init__.py`](QA-Test-Repo/avd_manager/__init__.py:3) defines these module-level attributes but they are absent from `__all__`; document whether `avd_manager.__version__` is part of the intended public API
- [x] 🔴 **TODO-CODE-045**: Document the inconsistency between the stated "Google-style docstring" convention and actual source — Section 6 states all public methods carry Google-style docstrings with `Args:` and `Returns:` sections, but inspection of every method in the codebase reveals only one-line docstrings; verify and correct this claim
- [x] 🔴 **TODO-CODE-046**: Document the `connect()` lazy-authentication pattern's failure mode — Section 6 describes the lazy-auth pattern but does not document what happens when `connect()` is called after a previous `connect()` already set `self.credential`; verify at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:43) that re-calling `connect()` re-creates all three SDK clients without re-authenticating
- [x] 🔴 **TODO-CODE-047**: Document the `host.name.split('/')[-1]` pattern's fragility — Section 6 documents this as a consistent pattern but does not note that it silently returns the full string if no `/` is present; verify the assumption holds for all Azure SDK response objects at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:104) and [`examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py:40)
- [x] 🔴 **TODO-CODE-048**: Verify the `get_logger()` duplicate-handler guard behaviour when `level` changes — the guard `if not logger.handlers` at [`avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py:19) means that if `get_logger('foo', logging.DEBUG)` is called after `get_logger('foo', logging.INFO)`, the level change is silently ignored; this is undocumented
- [x] 🔴 **TODO-CODE-049**: Document the `setup_file_logging()` root-logger attachment side effect — the function attaches a handler to the root logger at [`avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py:57), which means all loggers in the process (including third-party libraries) will write to the file; this global side effect is not documented
- [x] 🔴 **TODO-CODE-050**: Document the `Config` class thread-safety — `Config.set()` and `Config._load_config()` mutate `self.config` without any locking; if `Config` is shared across threads, race conditions are possible; this is not mentioned anywhere in the documentation
- [x] ⚠️ **TODO-CODE-051**: Add a code example for `AzureAuthenticator` standalone usage — Section 6 and Section 9 describe the wiring diagram but there is no code example showing how to use `AzureAuthenticator` with `ConnectionManager` as an alternative to `DefaultAzureCredential`; add an example to Section 12
- [x] ⚠️ **TODO-CODE-052**: Add a code example for `setup_file_logging()` — Section 4.2 describes the function but provides no usage example; add a snippet showing how to call it at application startup alongside `Config` initialisation
- [x] ⚠️ **TODO-CODE-053**: Add a code example for `Config.save()` — Section 5 documents the method but Section 12 provides no example of saving a modified configuration back to disk
- [x] ⚠️ **TODO-CODE-054**: Add a code example for `get_session_host_status()` — Section 5 documents the returned dict keys but Section 12 has no example showing how to call this method and interpret the result
- [x] ⚠️ **TODO-CODE-055**: Add a code example for `disconnect_user_session()` — Section 5 documents the method but Section 12 has no example showing how to obtain a `session_id` and pass it to this method
- [x] ⚠️ **TODO-CODE-056**: Document the `Config` class's handling of non-string YAML values — `yaml.safe_load()` at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:33) will parse integers and booleans natively; if a YAML file contains `subscription_id: 12345` (unquoted), the value will be an `int`, which may cause downstream type errors; this edge case is undocumented
- [x] ⚠️ **TODO-CODE-057**: Document the `PascalCase` vs `snake_case` convention enforcement — Section 6 states PEP 8 is mandated by `CONTRIBUTING.md` line 26, but `CONTRIBUTING.md` does not explicitly mention `PascalCase`/`snake_case` by name; verify the exact wording at [`CONTRIBUTING.md`](QA-Test-Repo/CONTRIBUTING.md:26)
- [x] ⚠️ **TODO-CODE-058**: Document the `@patch` decorator import-path convention — Section 11 shows `@patch('avd_manager.connection_manager.DefaultAzureCredential')` but does not explain why the patch target must be the module where the name is used (not where it is defined); add an explanatory note to the Testing Patterns section
- [x] ⚠️ **TODO-CODE-059**: Verify the `basic_usage.py` example's `Config()` constructor call — the example at [`examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py:8) calls `Config()` with no arguments and then immediately calls `config.set()` to inject values; this differs from the Section 12 canonical workflow which shows `Config('examples/config.yaml')`; reconcile the two examples
- [x] 📝 **TODO-CODE-060**: Document the `logger.debug` vs `logger.info` level choice in `Config.set()` — the method uses `logger.debug` at [`avd_manager/config.py`](QA-Test-Repo/config.py:69) while all other methods use `logger.info`; document why `set()` logs at DEBUG level (to avoid leaking sensitive values like `client_secret`)
- [x] 📝 **TODO-CODE-061**: Document the `list()` wrapping of Azure SDK paginators — `list_session_hosts()` and `get_user_sessions()` both wrap the SDK iterator in `list()` at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:29) and [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:46); document that this eagerly materialises all pages and may be slow for large host pools
- [x] 📝 **TODO-CODE-062**: Document the `load_dotenv()` call scope — `load_dotenv()` is called inside `Config.__init__()` at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:21), which means it runs every time a `Config` object is instantiated; document whether multiple instantiations cause any side effects
- [x] 🔴 **TODO-CODE-063**: Document the full authentication decision tree — Section 7 documents error handling but there is no flowchart or step-by-step description of the complete auth path: `Config` → `ConnectionManager.__init__()` → `connect()` → `authenticate()` → `DefaultAzureCredential()`; add this to Section 9
- [x] 🔴 **TODO-CODE-064**: Document the `ConnectionError` raised by `connect()` when `authenticate()` returns `False` — Section 7.2 lists this in the table but does not explain that this is the only method in the codebase that converts a boolean-return failure into a raised exception; verify at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:46)
- [x] 🔴 **TODO-CODE-065**: Document the `monitor_sessions()` propagation of `ConnectionError` — `monitor_sessions()` calls `list_session_hosts()` which raises `ConnectionError` if `avd_client` is `None`; however, `monitor_sessions()` itself has no guard and no try/except, so the `ConnectionError` propagates to the caller unhandled; this is not documented at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:100)
- [x] 🔴 **TODO-CODE-066**: Document the `get_host_pool()` return value when the pool does not exist — the method returns `None` on any exception at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:90), but the example at [`examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py:43) calls `pool_details.name` without a `None` check, which would raise `AttributeError`; document this unsafe pattern
- [x] 🔴 **TODO-CODE-067**: Document the `disconnect()` idempotency — calling `disconnect()` on an already-disconnected `ConnectionManager` is safe (setting `None` to `None`), but this is not documented; verify at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:92) and add a note
- [x] 🔴 **TODO-CODE-068**: Document the `connect()` partial-failure state — if `DesktopVirtualizationMgmtClient` instantiation succeeds but `ComputeManagementClient` raises an exception at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:53), `avd_client` will be set but `connect()` returns `False`; this leaves the object in a partially-connected state that is not documented
- [x] 🔴 **TODO-CODE-069**: Verify the `Config.validate()` error message format — the documentation in Section 5 states the error message "lists all missing fields"; confirm the exact format produced by `', '.join(missing_fields)` at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:74) matches what is described
- [x] 🔴 **TODO-CODE-070**: Document the `_load_config()` behaviour when `config_file` path does not exist — the condition at [`avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py:30) silently skips loading if the file is missing; no warning or error is logged; document this silent-skip behaviour
- [x] ⚠️ **TODO-CODE-071**: Document the `get_service_principal_credential()` `ValueError` message — the exact message `"Service principal credentials not configured"` is raised at [`avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py:46) but is not quoted in the documentation; add it to Section 7.2 for consistency with the `Config.validate()` entry
- [x] ⚠️ **TODO-CODE-072**: Document the `ConnectionError` message strings — Section 7.2 lists `ConnectionError` raises but does not quote the exact message strings used; verify `"Not connected to Azure services"` and `"Failed to authenticate"` at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:46) and [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:25) and add them to the table
- [x] ⚠️ **TODO-CODE-073**: Document the `get_session_host_status()` dict key `last_heartbeat` vs SDK attribute `last_heart_beat` — the method maps SDK attribute `last_heart_beat` to dict key `last_heartbeat` at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:89); this name transformation is not documented and could confuse callers comparing SDK docs to library output
- [x] ⚠️ **TODO-CODE-074**: Document the `list_host_pools()` SDK call — the method calls `avd_client.host_pools.list_by_resource_group()` at [`avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py:66); document which Azure SDK operation this maps to and what the returned objects look like
- [x] ⚠️ **TODO-CODE-075**: Document the `disconnect_user_session()` SDK call return value — the method calls `avd_client.user_sessions.disconnect()` at [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:63) and discards the return value; document whether the SDK call is synchronous and what the discarded return value contains
- [x] ⚠️ **TODO-CODE-076**: Document the `Config` class's behaviour when both a YAML file and environment variables provide the same key — Section 8 states "environment variables always take precedence" but does not show a concrete example; add a precedence example to Section 8
- [x] ⚠️ **TODO-CODE-077**: Verify the `basic_usage.py` example's `pool_details.host_pool_type` and `load_balancer_type` attribute access — these attributes are accessed at [`examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py:44) but are not documented anywhere in the code documentation; add a note about the Azure SDK object schema
- [x] ⚠️ **TODO-CODE-078**: Document the `SessionHandler` dependency on `connection_manager.resource_group` — `SessionHandler` directly reads `self.connection_manager.resource_group` in every method (e.g., [`avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py:31)); this tight coupling to a specific attribute is not mentioned in the dependency wiring section
- [x] ⚠️ **TODO-CODE-079**: Document the `get_logger()` `name` parameter type annotation gap — the function signature at [`avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py:7) uses `name: str` and `level: int` type hints but the documentation in Section 4.1 does not show the full typed signature; verify the type hints are present in source
- [x] 📝 **TODO-CODE-080**: Document the `setup.py` `find_packages()` scope — `find_packages()` at [`setup.py`](QA-Test-Repo/setup.py:9) will discover both `avd_manager` and `tests` packages; document whether `tests` is intentionally included in the distribution or if `find_packages(exclude=['tests'])` should be used
- [x] 📝 **TODO-CODE-081**: Document the `examples/basic_usage.py` `main()` function — the function is documented in Section 12 but the `main()` function itself has only a one-line docstring at [`examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py:6); verify the docstring matches the documented workflow
- [x] 📝 **TODO-CODE-082**: Document the `Config` class's `logging` and `host_pools` YAML sections — Section 8 notes these are "available for application-level use" but provides no code example showing `config.get('logging')` or `config.get('host_pools')`; add usage examples referencing [`examples/config.yaml`](QA-Test-Repo/examples/config.yaml:12)

## Architecture Verification
- [x] 🔴 **TODO-ARCH-001**: Add a deployment architecture diagram showing the runtime environment (developer workstation / CI runner → Azure Resource Manager endpoints) since no Dockerfile, CI pipeline, or cloud manifest exists in the repo — the current ASCII diagram only covers internal layers, not the actual deployment topology
- [x] 🔴 **TODO-ARCH-002**: Correct the architecture doc's claim that `AzureAuthenticator` is "imported but not yet used" by `ConnectionManager` — the code at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 8 confirms `AzureAuthenticator` **is** imported, but verify and explicitly document that it is never **called** anywhere inside the class body (lines 31–37 use `DefaultAzureCredential()` directly)
- [x] 🔴 **TODO-ARCH-003**: Document the undocumented `datetime` import in [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 3 — `datetime` is imported at module level but never referenced in any method; this is additional dead-import code that the architecture doc does not mention
- [x] 🔴 **TODO-ARCH-004**: Verify and document the exact line numbers for `Config.__init__()` — the architecture doc states line 14, but the actual file [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) shows `class Config` begins at line 12 and `__init__` at line 14; confirm all cited line numbers are still accurate after any edits
- [x] 🔴 **TODO-ARCH-005**: Add a sequence diagram for the end-to-end "happy path" flow illustrated in [`QA-Test-Repo/examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py) — `Config()` → `validate()` → `ConnectionManager()` → `connect()` → `list_host_pools()` → `SessionHandler()` → `monitor_sessions()` → `disconnect()` — no such diagram exists anywhere in the documentation
- [x] 🔴 **TODO-ARCH-006**: Document the discrepancy between `requirements.txt` (hard-pinned `==`) and `setup.py` (`>=` lower bounds) — the architecture doc correctly notes this but does not reference the specific lines: [`QA-Test-Repo/requirements.txt`](QA-Test-Repo/requirements.txt) lines 1–7 vs. [`QA-Test-Repo/setup.py`](QA-Test-Repo/setup.py) lines 11–18; add a risk note about the version mismatch between the two files
- [x] ⚠️ **TODO-ARCH-007**: Add a component interaction diagram showing the three-way relationship between `Config`, `ConnectionManager`, and `SessionHandler` — the current ASCII dependency graph shows import chains but not the runtime data-flow (e.g., `Config` values are manually passed into `ConnectionManager.__init__()` by the consumer; `Config` is never injected into `ConnectionManager` directly)
- [x] ⚠️ **TODO-ARCH-008**: Document that `README.md` references a `docs/` directory ([`QA-Test-Repo/README.md`](QA-Test-Repo/README.md) line 91) that does not exist in the repository file listing — the architecture doc's repository layout section should flag this phantom directory
- [x] ⚠️ **TODO-ARCH-009**: Add a sequence diagram for the authentication flow specifically — showing the `connect()` → `authenticate()` → `DefaultAzureCredential()` path at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) lines 40–60 and the separate, unused `AzureAuthenticator.get_credential()` path at [`QA-Test-Repo/avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py) lines 63–67
- [x] ⚠️ **TODO-ARCH-010**: Clarify in the architecture doc that `basic_usage.py` does **not** use `Config(config_file=...)` as shown in the README example — it calls `Config()` with no argument and then manually calls `config.set()` ([`QA-Test-Repo/examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py) lines 8–10); the two usage patterns are inconsistent and should be reconciled
- [x] ⚠️ **TODO-ARCH-011**: Add a state diagram for `ConnectionManager` showing the valid state transitions: `(uninitialized)` → `(authenticated)` → `(connected)` → `(disconnected)` — including the partial-connection failure state where `avd_client` is set but `network_client` remains `None`
- [x] ⚠️ **TODO-ARCH-012**: Document the `host_pools` and `logging` keys present in [`QA-Test-Repo/examples/config.yaml`](QA-Test-Repo/examples/config.yaml) lines 12–21 — these YAML keys (`logging.level`, `logging.file`, `host_pools`) are loaded by `yaml.safe_load()` into `self.config` but are never read by any code in the package; the architecture doc mentions `logging.file` but does not mention `logging.level` or `host_pools` as similarly unprocessed keys
- [x] ⚠️ **TODO-ARCH-013**: Expand the high-level design section to explicitly state that `ConnectionManager` does **not** accept a `Config` object — consumers must manually extract values via `config.get()` and pass them as positional arguments, as shown in [`QA-Test-Repo/examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py) lines 20–23; this is an architectural coupling gap worth documenting
- [x] 📝 **TODO-ARCH-014**: Add a note in the architecture overview about the absence of `pyproject.toml` and what that means for modern Python packaging toolchains (PEP 517/518 compliance) — the doc mentions no `pyproject.toml` exists but does not explain the implication for tools like `pip`, `build`, or `poetry`
- [x] 🔴 **TODO-ARCH-015**: Correct the architecture doc's description of `AzureAuthenticator.get_credential()` — the doc states it is at line 63 of [`QA-Test-Repo/avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py), but the actual method starts at line 62; verify all cited line numbers against the live file
- [x] 🔴 **TODO-ARCH-016**: Document the three distinct public methods of `AzureAuthenticator` that the architecture doc omits — `get_default_credential()` (line 27), `get_service_principal_credential()` (line 42), and `get_credential()` (line 62) in [`QA-Test-Repo/avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py); the doc only mentions `get_credential()` in the strategy pattern section
- [x] 🔴 **TODO-ARCH-017**: Add `get_session_host_status()` to the module responsibilities table for `SessionHandler` — the architecture doc lists it in the narrative (Section 2) but it is missing from the Module Responsibilities summary table; cross-reference [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 72
- [x] 🔴 **TODO-ARCH-018**: Document that `setup_file_logging()` in [`QA-Test-Repo/avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py) line 38 attaches a `FileHandler` to the **root logger** (not a named logger) — this means file logging will capture output from ALL loggers in the process, including third-party Azure SDK loggers, which is a significant undocumented side-effect
- [x] 🔴 **TODO-ARCH-019**: Document that `Config.save()` has no `output_file` default — it is a required positional argument at [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) line 75; the architecture doc describes it correctly but should also note there is no overwrite-protection or atomic write (no temp-file + rename pattern)
- [x] 🔴 **TODO-ARCH-020**: Add documentation for the `utils/__init__.py` re-export surface — [`QA-Test-Repo/avd_manager/utils/__init__.py`](QA-Test-Repo/avd_manager/utils/__init__.py) exports `get_logger` and `AzureAuthenticator` via `__all__`, but `setup_file_logging` is **not** re-exported from `utils`; this means consumers must import it directly from `avd_manager.utils.logger`, which is undocumented
- [x] 🔴 **TODO-ARCH-021**: Verify and document the exact `Config._load_config()` line range — the architecture doc cites lines 26–52 for [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py); confirm this is accurate against the current file (method starts at line 27 in the actual source)
- [x] ⚠️ **TODO-ARCH-022**: Add a note that `ConnectionManager.disconnect()` at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 91 does not raise or log a warning if called when already disconnected (all attributes are already `None`) — this is a silent no-op that could mask logic errors in consumer code
- [x] ⚠️ **TODO-ARCH-023**: Document the `host.name.split('/')[-1]` name-parsing pattern used in both [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 99 and [`QA-Test-Repo/examples/basic_usage.py`](QA-Test-Repo/examples/basic_usage.py) line 40 — this ARM resource ID parsing is an implicit convention that is not explained anywhere in the architecture doc and could silently produce wrong results if the SDK returns a short name instead of a full ARM path
- [x] ⚠️ **TODO-ARCH-024**: Document that `Config.validate()` only enforces `subscription_id` and `resource_group` as required fields — `tenant_id`, `client_id`, and `client_secret` are **not** validated even when service-principal auth is intended; reference [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) lines 63–73
- [x] ⚠️ **TODO-ARCH-025**: Add a note that `get_logger()` in [`QA-Test-Repo/avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py) line 8 sets `logger.propagate` to its default value of `True` — this means log records will propagate to the root logger and may be double-printed if the root logger also has a `StreamHandler` configured
- [x] ⚠️ **TODO-ARCH-026**: Document the `Config.set()` method's behavior when called with a key that shadows an environment-variable-loaded value — since `_load_config()` runs only at `__init__` time, a subsequent `config.set('subscription_id', 'new-value')` will permanently override the env-var value for the lifetime of the object; reference [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) lines 57–60
- [x] ⚠️ **TODO-ARCH-027**: Add the missing `test_session_handler.py` and `test_auth.py` file stubs to the repository layout diagram in the architecture doc — the current layout only shows the two existing test files; the absence of these test files is a documented gap that should be reflected in the layout
- [x] ⚠️ **TODO-ARCH-028**: Document that `ConnectionManager` exposes `avd_client`, `compute_client`, `network_client`, `subscription_id`, and `resource_group` as **public instance attributes** with no access control — `SessionHandler` accesses `connection_manager.avd_client` and `connection_manager.resource_group` directly; this tight coupling is noted in the doc but the specific attribute names accessed should be listed with line references to [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) lines 25, 30, 44, 49, 60, 65, 76, 81
- [x] 📝 **TODO-ARCH-029**: Add a note documenting that `CONTRIBUTING.md` references CI checks ("Ensure CI checks pass") at [`QA-Test-Repo/CONTRIBUTING.md`](QA-Test-Repo/CONTRIBUTING.md) line 43, but no CI configuration file (`.github/workflows/`, `azure-pipelines.yml`, etc.) exists in the repository — the architecture doc should flag this as an infrastructure gap
- [x] 🔴 **TODO-ARCH-030**: Correct the Strategy Pattern description — the architecture doc states `AzureAuthenticator` implements a strategy with "service-principal vs. default credential chain" selected at runtime, but the actual `get_credential()` method at [`QA-Test-Repo/avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py) lines 62–67 is a simple conditional, not a pluggable strategy object; document this as an **inline conditional** rather than a true Strategy pattern
- [x] 🔴 **TODO-ARCH-031**: Add documentation for the **dead import anti-pattern** present in two files: `AzureAuthenticator` imported but unused in [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 8, and `datetime` imported but unused in [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 3 — these should be called out as technical debt items in the design patterns section
- [x] 🔴 **TODO-ARCH-032**: Verify the Guard Clause pattern description — the doc states `ConnectionManager.list_host_pools()` checks `if not self.avd_client` at line 65; confirm this matches the actual guard at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) (actual guard is at line 65, raising `ConnectionError`) and that all five guarded methods in `SessionHandler` are similarly documented
- [x] 🔴 **TODO-ARCH-033**: Document the **inconsistent error-handling pattern** between `authenticate()` and `connect()` — `authenticate()` returns `False` on failure (line 37), while `connect()` raises `ConnectionError` for auth failure (line 45) but returns `False` for client-instantiation failure (line 60); reference [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) — this inconsistency is not captured in the current design patterns section
- [x] ⚠️ **TODO-ARCH-034**: Add a section documenting the **Eager Materialisation anti-pattern** more precisely — the doc mentions it in Section 8 but not in Section 3 (Design Patterns); both `list()` calls at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 68 and [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/session_handler.py) lines 28 and 47 should be cross-referenced as a consistent (if problematic) pattern
- [x] ⚠️ **TODO-ARCH-035**: Expand the Dependency Injection section to note that `SessionHandler` has **no interface or abstract base class** for `ConnectionManager` — the injection is duck-typed, meaning any object with an `avd_client` attribute and a `resource_group` attribute would satisfy the contract; reference [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 12
- [x] ⚠️ **TODO-ARCH-036**: Document the **Null Object / Safe Return** pattern's exact scope — the doc says "all data-retrieval methods" return `[]` or `None`, but `disconnect_user_session()` returns `False` (boolean) on error at [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 70; the return-type inconsistency (`[]` vs `None` vs `False`) should be explicitly noted
- [x] ⚠️ **TODO-ARCH-037**: Add a note that the Factory Function pattern for logging (`get_logger`) does not support log-level reconfiguration after initial creation — once a logger is created with `INFO`, calling `get_logger(name, logging.DEBUG)` on the same name will return the existing handler-attached logger unchanged due to the `if not logger.handlers` guard at [`QA-Test-Repo/avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py) line 19
- [x] ⚠️ **TODO-ARCH-038**: Add a **Context Manager pattern** gap note — `ConnectionManager` does not implement `__enter__`/`__exit__`, meaning it cannot be used as a `with` statement; consumers must manually call `disconnect()`, which is easy to forget; this is an architectural gap worth documenting
- [x] ⚠️ **TODO-ARCH-039**: Document the **partial-connection failure mode** as a named anti-pattern — if `DesktopVirtualizationMgmtClient` instantiation succeeds but `ComputeManagementClient` raises, `avd_client` is set while `compute_client` and `network_client` remain `None`, leaving the object in an inconsistent state; reference [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) lines 47–60
- [x] 📝 **TODO-ARCH-040**: Add a note that the Configuration Object pattern does not implement `__repr__` or `__str__` on `Config` — debugging configuration state requires inspecting `config.config` directly, which is an internal dict; reference [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py)
- [x] 📝 **TODO-ARCH-041**: Document whether the Facade pattern in `ConnectionManager` is intentionally limited to AVD operations only — `compute_client` and `network_client` are instantiated at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) lines 51–55 but no facade methods wrap them; the doc should state whether this is by design (future expansion) or an oversight
- [x] 🔴 **TODO-ARCH-042**: Correct the internal dependency graph — the doc shows `avd_manager.connection_manager.ConnectionManager` depends on `avd_manager.utils.auth (AzureAuthenticator — imported, unused)`, but the import at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 8 is a real compile-time dependency even if unused at runtime; clarify the distinction between import-time and call-time dependency
- [x] 🔴 **TODO-ARCH-043**: Add `requests` to the dependency graph with an explicit "imported: NO, used: NO" annotation — the architecture doc mentions it in the table (Section 4) but it does not appear in the dependency graph ASCII diagram; confirm by checking all source files that `import requests` never appears
- [x] ⚠️ **TODO-ARCH-044**: Add the `datetime` module to the dependency graph for `session_handler.py` — [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 3 imports `from datetime import datetime` but this is not reflected in the dependency graph; note it as an unused stdlib import
- [x] ⚠️ **TODO-ARCH-045**: Document the `pathlib.Path` stdlib dependency used in both [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) line 4 and [`QA-Test-Repo/avd_manager/utils/logger.py`](QA-Test-Repo/avd_manager/utils/logger.py) line 5 — these stdlib dependencies are absent from the dependency graph and the external dependencies table
- [x] ⚠️ **TODO-ARCH-046**: Verify the `azure-mgmt-desktopvirtualization==1.0.0` pin is still the correct/latest stable version — the architecture doc notes it is pinned but does not flag whether this version supports all SDK methods called (e.g., `user_sessions.disconnect()`, `session_hosts.get()`); add a verification note
- [x] 📝 **TODO-ARCH-047**: Add a note explaining why `azure-mgmt-compute` and `azure-mgmt-network` are listed as dependencies when no methods from those clients are called — document whether they are reserved for a planned feature or should be removed to reduce the install footprint; reference [`QA-Test-Repo/setup.py`](QA-Test-Repo/setup.py) lines 13–14
- [x] 🔴 **TODO-ARCH-048**: Document the missing `docs/` directory — `README.md` at [`QA-Test-Repo/README.md`](QA-Test-Repo/README.md) line 91 lists `docs/` in the project structure, but this directory does not exist in the repository; the architecture doc's repository layout should mark it as `(planned, not yet created)`
- [x] 🔴 **TODO-ARCH-049**: Add a note that there is no `MANIFEST.in` or explicit package data configuration in [`QA-Test-Repo/setup.py`](QA-Test-Repo/setup.py) — the `examples/config.yaml` and other non-Python files will not be included in a built distribution (`sdist`/`wheel`) without explicit inclusion rules
- [x] ⚠️ **TODO-ARCH-050**: Document the logging auto-wiring gap more precisely — [`QA-Test-Repo/examples/config.yaml`](QA-Test-Repo/examples/config.yaml) lines 12–14 define `logging.file: logs/avd_manager.log`, but `Config._load_config()` at [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) lines 27–52 only maps five specific keys; the `logging` nested dict is loaded into `self.config` but never acted upon — add a concrete code reference to show exactly where the wiring is absent
- [x] ⚠️ **TODO-ARCH-051**: Add a note about the absence of a `pytest` or `tox` configuration — the only test runner documented is `python -m unittest discover tests` (in `CONTRIBUTING.md` and `README.md`); no `pytest.ini`, `tox.ini`, `setup.cfg [tool:pytest]`, or `pyproject.toml [tool.pytest]` exists; document this as a testing infrastructure gap
- [x] ⚠️ **TODO-ARCH-052**: Document the absence of a `.gitignore` file in the repository listing — there is no `.gitignore` visible in the file list, meaning `.env` files, `__pycache__/`, `*.pyc`, `logs/`, and `venv/` directories could be accidentally committed; this is a security and hygiene gap
- [x] ⚠️ **TODO-ARCH-053**: Add a note that `setup.py` does not declare a `long_description` from `README.md` — [`QA-Test-Repo/setup.py`](QA-Test-Repo/setup.py) has no `long_description` or `long_description_content_type` field, meaning a PyPI upload would show no package description
- [x] 📝 **TODO-ARCH-054**: Document the Python version support gap — `setup.py` at [`QA-Test-Repo/setup.py`](QA-Test-Repo/setup.py) lines 23–25 lists classifiers for Python 3.8, 3.9, and 3.10 only, but `python_requires=">=3.8"` would allow installation on 3.11 and 3.12; document whether 3.11/3.12 are intentionally unsupported or simply untested
- [x] 🔴 **TODO-ARCH-055**: Add concrete line references to the scalability section for every bottleneck claim — the `monitor_sessions()` O(n) bottleneck should cite [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) lines 95–102; the eager list materialisation should cite lines 28–32 and 45–50 in the same file; the no-retry claim should cite the bare `except Exception` blocks throughout both `connection_manager.py` and `session_handler.py`
- [x] ⚠️ **TODO-ARCH-056**: Document the error-propagation asymmetry between `ConnectionError` (raised for pre-condition failures) and swallowed `Exception` (for Azure API failures) — the architecture doc mentions this in Section 6 but does not provide a complete table of which methods raise vs. swallow; create a method-by-method error-behaviour reference table citing [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) and [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py)
- [x] ⚠️ **TODO-ARCH-057**: Add a note that `disconnect_user_session()` at [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) lines 56–70 is a **write/mutating operation** — unlike all other methods which are read-only queries, this method modifies Azure state; the architecture doc should distinguish read vs. write operations in the communication patterns section
- [x] ⚠️ **TODO-ARCH-058**: Document that `list()` materialisation of Azure SDK paginators at [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 68 and [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) lines 28 and 47 will block the calling thread for the full duration of pagination — there is no timeout, no page-size limit, and no streaming; document the maximum expected response time implications
- [x] 📝 **TODO-ARCH-059**: Add a note that the `requests` library (listed in [`QA-Test-Repo/requirements.txt`](QA-Test-Repo/requirements.txt) line 7) is not used for any communication — all HTTP is handled internally by the Azure SDK via `azure-core`; document whether `requests` is a transitive dependency that was accidentally promoted to a direct dependency
- [x] 🔴 **TODO-ARCH-060**: Add a concrete code reference for the `Config.save()` secret-leakage risk — the architecture doc correctly flags it but should cite the exact lines: [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) lines 75–78 show `yaml.dump(self.config, f)` with no filtering; add a recommended mitigation (e.g., scrub `client_secret` before serialisation)
- [x] 🔴 **TODO-ARCH-061**: Document that `ConnectionManager.authenticate()` logs `f"Initialized ConnectionManager for subscription: {subscription_id}"` at INFO level — [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) line 29 logs the subscription ID to stdout by default; the security section should explicitly list all fields that appear in log output (subscription ID, host pool names, session host names, session IDs)
- [x] 🔴 **TODO-ARCH-062**: Correct the security section's description of `AzureAuthenticator` — the doc states the strategy selector is at line 63 of [`QA-Test-Repo/avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py); verify the actual line number and also document that `get_service_principal_credential()` raises `ValueError` (not returns `None`) when credentials are incomplete (line 46)
- [x] ⚠️ **TODO-ARCH-063**: Add a note that `load_dotenv()` in [`QA-Test-Repo/avd_manager/config.py`](QA-Test-Repo/avd_manager/config.py) line 20 loads `.env` from the **current working directory** at the time `Config()` is instantiated — if the library is used from a different working directory than the project root, the `.env` file will not be found; document this as a deployment-context sensitivity
- [x] ⚠️ **TODO-ARCH-064**: Document the `DefaultAzureCredential` fallback chain order explicitly in the security section — the architecture doc lists the chain in Section 7 but should reference the actual `azure-identity==1.15.0` documentation order: environment variables → workload identity → managed identity → Azure CLI → Azure PowerShell → Azure Developer CLI → interactive browser; confirm this matches the pinned version's behaviour
- [x] ⚠️ **TODO-ARCH-065**: Add a note that there is no token refresh or credential expiry handling in `ConnectionManager` — once `self.credential` is set via `DefaultAzureCredential()`, it is reused for all subsequent SDK calls; `DefaultAzureCredential` handles token refresh internally, but this behaviour is undocumented in the architecture; reference [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) lines 24–25
- [x] ⚠️ **TODO-ARCH-066**: Document the RBAC roles required for each operation — the architecture doc states "appropriate roles" are needed but does not specify which Azure built-in roles are required for `list_host_pools()`, `get_host_pool()`, `list_session_hosts()`, `get_user_sessions()`, and `disconnect_user_session()`; add a minimum-privilege role mapping table
- [x] 📝 **TODO-ARCH-067**: Add a note that `AzureAuthenticator` stores the credential as `self.credential` (instance attribute) after `get_credential()` is called — [`QA-Test-Repo/avd_manager/utils/auth.py`](QA-Test-Repo/avd_manager/utils/auth.py) lines 34 and 52 — meaning the credential object (which may contain token cache) persists in memory for the lifetime of the `AzureAuthenticator` instance; document the memory/security implication
- [x] 🔴 **TODO-ARCH-068**: Add concrete line references to the scalability section for every bottleneck claim — the `monitor_sessions()` O(n) bottleneck should cite [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) lines 95–102; the eager list materialisation should cite lines 28–32 and 45–50 in the same file; the no-retry claim should cite the bare `except Exception` blocks throughout both `connection_manager.py` and `session_handler.py`
- [x] ⚠️ **TODO-ARCH-069**: Document the `active_sessions` dead-code item with a recommended resolution — the architecture doc correctly identifies `self.active_sessions = {}` at [`QA-Test-Repo/avd_manager/session_handler.py`](QA-Test-Repo/avd_manager/session_handler.py) line 18 as dead code, but should recommend either: (a) removing it, or (b) implementing it as a local cache for `monitor_sessions()` results to avoid repeated API calls; add this as a concrete actionable item
- [x] 📝 **TODO-ARCH-070**: Add a future-roadmap subsection to the scalability section documenting the intended use of `compute_client` and `network_client` — since both clients are instantiated but never used (confirmed by searching all source files), the architecture doc should state the planned operations (e.g., VM power state management via `compute_client`, NIC/IP queries via `network_client`) to distinguish intentional placeholders from dead code; reference [`QA-Test-Repo/avd_manager/connection_manager.py`](QA-Test-Repo/avd_manager/connection_manager.py) lines 51–55

## Structure Verification
- [x] 🔴 **TODO-STRUCT-001**: Verify and correct the `README.md` directory tree — it lists a `docs/` directory that **does not exist** in the actual repository (confirmed by `list_files`); the structure doc must flag this phantom entry and remove it from §2
- [x] 🔴 **TODO-STRUCT-002**: Document the total file count discrepancy — the structure doc states "16 files across 5 logical areas" but `list_files` returns exactly 16 entries; confirm the count is correct and add a note that `docs/` is absent despite appearing in `README.md`
- [x] ⚠️ **TODO-STRUCT-003**: Add rationale for why `examples/config.yaml` lives inside `examples/` rather than at the project root alongside `requirements.txt` and `setup.py`, since `examples/basic_usage.py` calls `Config()` with no file argument (not `Config('examples/config.yaml')`)
- [x] ⚠️ **TODO-STRUCT-004**: Document the absence of a `.env.example` or `.env` template file — `README.md` instructs users to create a `.env` file, `config.py` calls `load_dotenv()`, but no `.env` template exists in the repo; the structure doc should note this gap
- [x] ⚠️ **TODO-STRUCT-005**: Document the absence of a `CHANGELOG.md` or `HISTORY.md` file — the project is versioned at `0.1.0` but no change log exists; the structure doc should note this as a missing conventional project-root file
- [x] ⚠️ **TODO-STRUCT-006**: Document the absence of a `LICENSE` file — `README.md` declares "MIT License" but no `LICENSE` or `LICENSE.txt` file is present in the repository; the structure doc should flag this inconsistency
- [x] ⚠️ **TODO-STRUCT-007**: Clarify the `examples/` directory isolation rationale more precisely — the structure doc says examples are separated "so they are never accidentally imported," but `examples/basic_usage.py` imports directly from `avd_manager`, meaning it requires the package to be installed or the repo root on `sys.path`; document the required execution context
- [x] 📝 **TODO-STRUCT-008**: Add file sizes to the directory layout table for all files not already covered in §4 — `tests/__init__.py` (33 bytes), `avd_manager/utils/__init__.py` (155 bytes), `tests/test_config.py` (1,780 bytes), `tests/test_connection_manager.py` (1,829 bytes), `requirements.txt` (168 bytes), `CONTRIBUTING.md` (1,507 bytes), and `README.md` (2,729 bytes) are all missing from §4
- [x] 📝 **TODO-STRUCT-009**: Add a "File Count by Layer" summary table to §2 (e.g., `avd_manager/` = 7 files, `tests/` = 3 files, `examples/` = 2 files, root = 4 files) to make the "16 files" claim immediately verifiable
- [x] 📝 **TODO-STRUCT-010**: Document the `avd_manager/utils/__init__.py` file in §4 — it is described in §9 but has no dedicated entry in the Key Files section; its re-export pattern and `__all__` declaration deserve explicit coverage
- [x] 📝 **TODO-STRUCT-011**: Document `tests/__init__.py` more fully in §4 — the structure doc only mentions it contains a module docstring, but its role in enabling `unittest discover` and its 33-byte size should be called out explicitly as a Key File entry
- [x] 📝 **TODO-STRUCT-012**: Verify and document whether a `venv/` or `.venv/` virtual-environment directory is expected at the project root — `CONTRIBUTING.md` instructs contributors to create `venv/` via `python -m venv venv`; the structure doc should note this runtime-only directory and explain it is excluded from version control
- [x] 🔴 **TODO-STRUCT-013**: Correct the `auth.py` API description in §3.2 and §4 — the structure doc states `AzureAuthenticator` exposes a single `get_credential()` entry point, but the actual class has **three** public methods: `get_credential()`, `get_default_credential()`, and `get_service_principal_credential()`; all three must be documented
- [x] 🔴 **TODO-STRUCT-014**: Correct the `connection_manager.py` dependency description — §3.3 and §4 state that `ConnectionManager.authenticate()` uses `DefaultAzureCredential` directly and that `AzureAuthenticator` is "available" but not used; the source confirms `AzureAuthenticator` **is imported** (`from .utils.auth import AzureAuthenticator`) but `authenticate()` still calls `DefaultAzureCredential()` directly — document this as an architectural inconsistency (dead import)
- [x] 🔴 **TODO-STRUCT-015**: Document the `datetime` import in `session_handler.py` — `from datetime import datetime` is present at the top of the file but `datetime` is **never used** anywhere in the module; the structure doc must note this unused import as a code smell and flag it for cleanup
- [x] 🔴 **TODO-STRUCT-016**: Document the `self.active_sessions = {}` instance attribute in `SessionHandler.__init__` — this dict is initialized but **never populated or read** by any method in the class; the structure doc's description of `SessionHandler` in §3.2 and §4 must acknowledge this undocumented/unused state
- [x] ⚠️ **TODO-STRUCT-017**: Expand the dependency flow diagram in §3.3 to show that `session_handler.py` imports `datetime` from the standard library — the current diagram omits all stdlib imports; at minimum `datetime`, `logging`, `sys`, `os`, and `pathlib` should be represented
- [x] ⚠️ **TODO-STRUCT-018**: Clarify the `config.py` → `utils/logger` dependency direction in §3.3 — the arrow in the diagram points from `Config` to `utils/logger` but the label reads "──────────────────────────────────────────►" which is visually ambiguous; replace with the same two-line format used for `ConnectionManager`
- [x] ⚠️ **TODO-STRUCT-019**: Document the `pathlib.Path` usage in both `config.py` and `logger.py` — `config.py` uses `Path` to check if a config file exists before opening it, and `logger.py` uses `Path` to auto-create log directories; the structure doc's §9 shared-code section should note `pathlib` as a shared stdlib dependency pattern
- [x] ⚠️ **TODO-STRUCT-020**: Document the `os` module usage in `config.py` — `_load_config()` calls `os.getenv()` five times for the five Azure environment variables; the dependency map in §10 should list `stdlib: os, pathlib, yaml` under `Config`
- [x] ⚠️ **TODO-STRUCT-021**: Verify and document the `requests` package usage — `requests==2.31.0` appears in both `requirements.txt` and `setup.py` `install_requires`, but **no file in the codebase imports `requests`**; the structure doc §6 table notes it as "available for future use" — confirm this is intentional and add a note explaining why it is a declared dependency with no current consumer
- [x] ⚠️ **TODO-STRUCT-022**: Document the `host.name.split('/')[-1]` name-parsing pattern — both `examples/basic_usage.py` (line: `pool_name = host_pools[0].name.split('/')[-1]`) and `session_handler.py` `monitor_sessions()` (line: `host_name = host.name.split('/')[-1]`) use this pattern to strip the ARM resource path prefix; the structure doc should explain this shared convention and where it originates
- [x] 📝 **TODO-STRUCT-023**: Add `stdlib: logging, sys, pathlib` to the `utils/logger.py` node in the §10 dependency map — the current map only lists `stdlib: logging, sys, pathlib` as a trailing note but does not show it as a formal dependency edge
- [x] 📝 **TODO-STRUCT-024**: Document the `config.py` stdlib dependency on `os` in the §10 dependency map — the current map lists `pyyaml`, `python-dotenv`, and `utils.logger` under `Config` but omits `stdlib: os, pathlib`
- [x] 🔴 **TODO-STRUCT-025**: Correct the `examples/basic_usage.py` lifecycle description in §7 — the structure doc states step 1 is "Instantiate `Config` and set `subscription_id` / `resource_group`," but the actual code calls `Config()` with **no file argument** (not `Config('config.yaml')` as shown in `README.md`); document this discrepancy between the example script and the README usage snippet
- [x] 🔴 **TODO-STRUCT-026**: Document the `get_host_pool()` and `get_session_host_status()` calls in `examples/basic_usage.py` — §7 lists only 6 lifecycle steps but the script also calls `get_host_pool(pool_name)` and accesses `pool_details.host_pool_type` / `pool_details.load_balancer_type`; these steps are missing from the documented flow
- [x] ⚠️ **TODO-STRUCT-027**: Document the `disconnect_user_session()` method as a missing entry point in the usage lifecycle — `SessionHandler.disconnect_user_session()` is a key public method listed in §3.2 but is **never demonstrated** in `examples/basic_usage.py` or in the `README.md` usage snippet; the structure doc should note this gap and suggest a second example
- [x] ⚠️ **TODO-STRUCT-028**: Document the `get_session_host_status()` method as a missing usage example — this method is described in §3.2 and §4 but is not called anywhere in `examples/basic_usage.py`; the structure doc should flag it as an undocumented entry point from a consumer perspective
- [x] ⚠️ **TODO-STRUCT-029**: Clarify the `connect()` vs `authenticate()` entry point relationship — `connect()` lazily calls `authenticate()` internally, meaning `authenticate()` is effectively an internal step, not a separate public entry point; the structure doc §7 lifecycle should make clear that consumers call only `connect()` and that `authenticate()` is an implementation detail
- [x] ⚠️ **TODO-STRUCT-030**: Document the `AzureAuthenticator` entry point for advanced auth scenarios — §7 mentions only `ConnectionManager` for authentication, but `AzureAuthenticator` in `utils/auth.py` is a fully public class (re-exported from `avd_manager.utils`) with its own `get_credential()` entry point; document how a consumer would use it directly
- [x] ⚠️ **TODO-STRUCT-031**: Document the `setup_file_logging()` entry point from `avd_manager/utils/logger.py` — this function is described in §4 and §9 but is never shown being called in `examples/basic_usage.py` or in any test; the structure doc should show where and how a consumer would invoke it (e.g., reading `logging.file` from `Config` and passing it to `setup_file_logging()`)
- [x] ⚠️ **TODO-STRUCT-032**: Document the `Config.save()` method as a public entry point — `save(output_file)` is implemented in `config.py` and described in §4 but is not listed in the §7 lifecycle or demonstrated in any example; add it to the documented public API surface
- [x] 📝 **TODO-STRUCT-033**: Add a note in §7 that there is no `pip install avd-manager` PyPI entry point — the package is not published to PyPI; consumers must install via `pip install -e .` or `pip install -r requirements.txt` from a local clone; this is implied but never stated explicitly
- [x] 📝 **TODO-STRUCT-034**: Document the `if __name__ == '__main__': unittest.main()` guards in both `tests/test_config.py` and `tests/test_connection_manager.py` as secondary test entry points — §7 only mentions `python -m unittest discover tests` but individual test files can also be run directly
- [x] 🔴 **TODO-STRUCT-035**: Document the `logging` and `host_pools` YAML keys in `config.py` — `examples/config.yaml` defines `logging.level`, `logging.file`, and `host_pools` list entries, but `config.py`'s `_load_config()` method **only maps the five Azure credential keys** via `os.getenv()`; the `logging` and `host_pools` keys are loaded from YAML but never validated or accessed via `Config.get()` in any module — the structure doc must flag this as undocumented behavior
- [x] 🔴 **TODO-STRUCT-036**: Correct the `setup.py` `install_requires` version specifier documentation in §6 — `setup.py` uses `>=` (minimum version) specifiers (e.g., `azure-identity>=1.15.0`) while `requirements.txt` uses `==` (exact pinned) versions; the structure doc §6 table currently shows only the pinned versions from `requirements.txt` and does not distinguish between the two files' differing constraint styles
- [x] ⚠️ **TODO-STRUCT-037**: Document the `Config.validate()` scope limitation — `validate()` only checks `subscription_id` and `resource_group`; `tenant_id`, `client_id`, and `client_secret` are loaded but **never validated** even when service-principal auth is intended; the structure doc §4 description of `validate()` should note this partial validation behavior
- [x] ⚠️ **TODO-STRUCT-038**: Document the `setup.py` missing metadata fields — `setup.py` includes `author_email = "qa@example.com"` which is not mentioned anywhere in the structure doc §6; also missing from the §6 table are `description` ("Azure Virtual Desktop Management Tool") and `author_email`
- [x] ⚠️ **TODO-STRUCT-039**: Document the `setup.py` missing `long_description`, `url`, and `license` fields — these are standard `setuptools` fields absent from `setup.py`; the structure doc §6 should note these omissions as gaps in the build metadata, especially since `README.md` declares an MIT license
- [x] ⚠️ **TODO-STRUCT-040**: Document the version pinning strategy discrepancy between `requirements.txt` and `setup.py` — `requirements.txt` pins exact versions (`==`) for reproducible installs while `setup.py` uses minimum-version constraints (`>=`) for flexible library compatibility; the structure doc §6 should explain this intentional dual-file strategy
- [x] 📝 **TODO-STRUCT-041**: Document the `examples/config.yaml` location note — the file lives in `examples/` but is described as a "Runtime Configuration Schema" in §6; clarify that it is a **template/example only** and that a real runtime config file would be placed at the project root or a user-specified path passed to `Config(config_file=...)`
- [x] 🔴 **TODO-STRUCT-042**: Document the missing test coverage for `session_handler.py` — there is **no `tests/test_session_handler.py`** file in the repository; the structure doc §8 coverage table lists only `TestConfig` and `TestConnectionManager` but should explicitly flag `SessionHandler`, `AzureAuthenticator`, and `get_logger`/`setup_file_logging` as untested modules
- [x] ⚠️ **TODO-STRUCT-043**: Document the missing test coverage for `avd_manager/utils/auth.py` — `AzureAuthenticator` has no corresponding test file; the structure doc §8 should note that `get_credential()`, `get_default_credential()`, and `get_service_principal_credential()` are all untested
- [x] ⚠️ **TODO-STRUCT-044**: Document the missing test coverage for `avd_manager/utils/logger.py` — `get_logger()` and `setup_file_logging()` have no test file; the structure doc §8 should note the duplicate-handler guard (`if not logger.handlers`) and the `mkdir(parents=True, exist_ok=True)` behavior as untested code paths
- [x] ⚠️ **TODO-STRUCT-045**: Correct the `test_connection_manager.py` coverage description in §8 — the structure doc says `TestConnectionManager` covers `connect()` but the actual test file has **no `test_connect` method**; the four actual test methods are `test_initialization`, `test_authenticate_success`, `test_authenticate_failure`, and `test_disconnect`; `connect()`, `list_host_pools()`, and `get_host_pool()` are not tested
- [x] 📝 **TODO-STRUCT-046**: Document the `mock_open` import in `tests/test_config.py` — `from unittest.mock import patch, mock_open` is present but `mock_open` is **never used** in any test method in the file; the structure doc §8 notes it as "available for YAML file mocking" but should flag it as an unused import that signals incomplete test coverage for `Config._load_config()` with a real YAML file

